# Napkin UI feature map

Code-traceable operating map for agents driving the Expo app. Route inventory is based on default exports under `napkin-app/app/`; data ownership is traced through `napkin-app/hooks/`, `napkin-app/lib/queryKeys.ts`, and `supabase/functions/`. Paths in this document are repository-relative.

## 1. Verification protocol

### Launch and observe

1. Use an iOS Simulator with the Napkin development client (`com.majilaii.dining-journal-app`). The bundle identifier is declared in `napkin-app/app.config.ts`.
2. In `napkin-app/`, run `npm start` to start Metro for an already-installed development client (`napkin-app/package.json`).
3. If the development client is absent or native code/patches changed, run `npm run ios` for a fresh native build (`napkin-app/package.json`). `postinstall` applies `patch-package`, so install dependencies before judging native-patch behavior.
4. A cold launch shows the native splash (the Napkin wordmark on paper), then the launch screen (`components/launch/`, section 3) until connectivity, session restore, the onboarding-gate read and routing have all landed. `app/_layout.tsx::RootLayoutNav` chooses auth, onboarding, or the signed-in shell underneath it. `/` itself redirects to `/(tabs)/places` (`app/index.tsx`).
5. Navigate to every changed state, not merely every changed route. Capture a screenshot of each loading, content, empty, error, permission, privacy, and modal state affected by the change.
6. For a first-screen touch check, tap controls near the bottom of the first native screen mounted after launch; see TICKET-212 in section 5.

### Live-data rule

Live verification is **READ-ONLY**. Do not create, edit, react, follow, invite, RSVP, import, moderate, delete, or change settings by tapping through a live account. Exercise write paths in unit/integration tests with isolated fixtures. The safety labels in section 4 describe blast radius; even a `SAFE` control is not permission to mutate live verification data.

Reading screens, switching local tabs/filters, opening and dismissing sheets, moving/zooming a map, and taking screenshots are allowed. Treat OS permission prompts as device mutation and avoid them unless the test device is disposable (`hooks/useNearbyLocation.ts`, `components/notifications/NotifPermissionSheet.tsx`).

### Sign-in data

- `scripts/seed/demo-accounts.ts` provisions two review/demo identities through normal production Edge Function paths, completes onboarding, makes them mutual follows, creates a Table, accepts membership, and adds representative entries.
- The script reads email/password values from `DEMO_EMAIL_A`, `DEMO_EMAIL_B`, `DEMO_PASSWORD_A`, and `DEMO_PASSWORD_B`; credentials are intentionally not stored in this map.
- Use an already-provisioned authorized review account. Do **not** run the seed script during read-only verification: it writes live profiles, follows, membership, and entries.
- If no authorized review identity is available, stop rather than inventing credentials or signing up a live user. Auth UI behavior can still be covered by tests around `app/auth.tsx` and `providers/AuthProvider.tsx`.

### Shell and route-count conventions

- Five layout modules shape navigation but are not counted as pages: `app/_layout.tsx`, `app/(tabs)/_layout.tsx`, `app/admin/_layout.tsx`, `app/onboarding/_layout.tsx`, and `app/settings/_layout.tsx`.
- There are **70 default-export `.tsx` modules** under `app/`: five layouts plus **65 intended route modules**. The onboarding progress support component lives under `components/onboarding/`; `/welcome` adds the replayable guide.
- `app/onboarding/OnboardingDraftContext.tsx` and `app/onboarding/styles.ts` are support modules, not routes.
- Root Stack configuration mentions `/day/[date]`, but no `app/day/[date].tsx` exists (`app/_layout.tsx`); it is a dangling registration, not a drivable route.
- The custom signed-in navigation is `app/_layout.tsx::BottomNavBar`: **FEED · TABLE · PLACES · PROFILE**. Expo's built-in tab bar is hidden in `app/(tabs)/_layout.tsx`; the legacy `/search` redirect marks PLACES active, while pushed `/places-scope` screens intentionally have no bottom nav.

## 2. Page map

Notation: `EF` = Supabase Edge Function; `RPC` = PostgREST function. Table names are the storage touched by the named hook/function, not a promise that every table renders visibly.

### Shell, primary tabs, and discovery

| Route | Component file | Feeding hook(s) / client | EF / RPC | Backing tables |
|---|---|---|---|---|
| `/` | `napkin-app/app/index.tsx` | `Redirect` | — | — |
| `/feed` | `napkin-app/app/(tabs)/feed.tsx` | `useFriendsFeed`, `useSocials`, `useFollowCandidates`, `useBrowsePublicLists` | `feed-friends` (activity opt-in) / `fn_friends_activity`; `feed-socials`; `user-profile`; `lists` | `entries`, `entry_photos`, `post_reactions`, `profiles`, `restaurants`, `clip_thumbs`, `lists`, `list_entries`, `follows`, `wishlist_items`, `blocked_users` |
| `/journal` | `napkin-app/app/(tabs)/journal.tsx` | `useMySoloEntries`, `useUnreadCount` | RPC `fn_my_solo_entries`; EF `notifications` | `entries`, `entry_tables`, `restaurants`, `profiles`, `notifications` |
| `/log` | `napkin-app/app/(tabs)/log.tsx` | none; blank legacy placeholder | — | — |
| `/profile` | `napkin-app/app/(tabs)/profile.tsx` → `components/profile/ProfileScreenBody.tsx` | `useUserProfile`, `useUserSpots`, `useLedger`, list and import hooks | `user-profile`, `lists`, `wishlist`; RPC `fn_visible_entry_ids` | `profiles`, `entries`, `entry_photos`, `follows`, `lists`, `list_entries`, `table_members`, `tables`, `user_top_4`, `user_profile_takes`, `import_jobs` |
| `/places` | `napkin-app/app/(tabs)/places.tsx` → `components/places/PlacesScreen.tsx` | `useRestaurantSearch`, `useMyWishlist`, `useUserSpots`, scope-gated `useNetworkMapPins`, `useTableWishlist`, `useTableMapPins`, active-scope `useTableMembers`, `useTables`, list/search/locality/clip hooks; shared `placesScreenState` | `places-search`, `restaurant-history?action=search`, `wishlist`, `table-atlas`, `table-management`, `user-profile`, `lists`, `restaurant-completeness`; RPC `fn_visible_entry_ids`; local App-Group import manifests | `restaurants`, `entries`, `profiles`, `follows`, `wishlist_items`, `lists`, `list_entries`, `tables`, `table_members`, `suppers`, `supper_members`, `import_jobs`, `restaurant_completeness_queue` |
| `/places-scope` | `napkin-app/app/places-scope.tsx` → `components/places/PlacesScreen.tsx` | locked independent `placesScreenState` instance; same Places hooks with Table overlap/been and members gated to the active Table | `wishlist?action=list_table`, `table-atlas?action=map_pins`, `table-management`; world search remains `places-search` + `restaurant-history?action=search` | `wishlist_items`, `restaurants`, `tables`, `table_members`, `entries`, `suppers`, `supper_members` |
| `/search` | `napkin-app/app/(tabs)/search.tsx` | param-preserving `Redirect` (`q`, `mode`) to `/(tabs)/places` | — | — |
| `/tables` | `napkin-app/app/(tabs)/tables.tsx` | `useTables`, `useTableActivity`, `useTableAtlas`, `useTableMembers`, `useTableTopFour`, `TableLedgerModule` → `useLedger` | `table-management`, `table-activity`, `table-atlas`, `user-profile?action=ledger` | `tables`, `table_members`, `entries`, `entry_photos`, `restaurants`, `profiles`, `suppers`, `supper_members`, `gatherings`, `gathering_rsvps`, `table_shares`, `wishlist_items`, `user_top_4`, `blocked_users`, legacy `table_nights` |

### Auth and onboarding

| Route | Component file | Feeding hook(s) / client | EF / RPC | Backing tables |
|---|---|---|---|---|
| `/auth` | `napkin-app/app/auth.tsx`; `components/auth/AppleSignInButton.tsx` | `useAuth`; Supabase Auth password/OAuth clients; `lib/oauth.ts`; `lib/pendingIdentity.ts` | Supabase Auth (`signInWithIdToken`, then `updateUser` to persist the provider name) | Auth identities; `auth.users.raw_user_meta_data.full_name`; profile gate is later in `providers/AuthProvider.tsx` |
| `/reset-password` | `napkin-app/app/reset-password.tsx` | Supabase Auth session and password update | Supabase Auth | Auth identities |
| `/onboarding` | `napkin-app/app/onboarding/index.tsx` | `OnboardingDraftContext`, `hooks/onboarding/useProvidedDisplayName`, `lib/onboardingName.ts`, `lib/pendingIdentity.ts`, auth metadata | none until final completion | Auth identity metadata is read only; draft is local React state. The step renders ONLY when no provider supplied a name |
| `/onboarding/photo` | `napkin-app/app/onboarding/photo.tsx` | draft context, image staging/moderation helpers | `moderate-image`; object upload | profile-image staging/storage objects |
| `/onboarding/city` | `napkin-app/app/onboarding/city.tsx` | `HomeCityPicker` local city search, `useCoDiners`, draft context, completion client | `user-profile?action=co_diners/complete_onboarding`; RPC `fn_complete_onboarding` | `profiles`, `follows` |
| `/onboarding/follows` | `napkin-app/app/onboarding/follows.tsx` | co-diner/follow clients and draft completion | `user-profile?action=co_diners/follow/complete_onboarding` | `profiles`, `follows` |
| `/welcome` | `napkin-app/app/welcome.tsx`; `components/onboarding/guideContent.ts`, `components/onboarding/GuideIllustration.tsx`, `ShareWalkthrough.tsx`, `share-demo/{SourceAppScene,SystemSharePanels,NapkinDemoScene,demoContent}` | auth identity and completed-profile gate; `lib/discoveryGuide.ts` for optional local tips | none | versioned per-account AsyncStorage preferences; bundled example assets |

### Entries, people, and restaurant detail

| Route | Component file | Feeding hook(s) / client | EF / RPC | Backing tables |
|---|---|---|---|---|
| `/create-entry` | `napkin-app/app/create-entry.tsx` | `useCreateEntry`, `useTables`, `useUserSearch`, `useRecentCompanions`, recent-post query, supper/merge/round helpers | `entry`, `user-profile`, `places-search`, `table-management`, `table-night` (legacy) | `entries`, `entry_photos`, `entry_companions`, `entry_participants`, `entry_tables`, `restaurants`, `profiles`, `follows`, `blocked_users`, `tables`, `table_members`, `suppers`, `supper_members`, legacy `table_nights`/`round_entries` |
| `/log-meal` | `napkin-app/app/log-meal.tsx` | `useCreateEntry`, `useVisitReviewDraft`, `useAvailableVisitCheckIns`, `useRestaurantVisitMutations`, `useTables`, `useUserSearch`, `useRecentCompanions`, supper attach/add helpers | `entry`, `restaurant-history`, `user-profile`, `table-management` | `entries`, `entry_photos`, `entry_companions`, `entry_tables`, `tables`, `table_members`, `suppers`, `supper_members`, `profiles`, `follows`, `blocked_users` |
| `/entry-detail` | `napkin-app/app/entry-detail.tsx` | direct entry query, `useRestaurantHistory`, `usePostInteractions`, `useUserSearch`, `useRecentCompanions`, supper/legacy-round helpers | `restaurant-history`, `post-interactions`, `user-profile`, `entry`, `account`, legacy `table-night`; RPC `is_entry_publicly_eligible` | `entries`, `entry_tables`, `entry_photos`, `entry_companions`, `profiles`, `follows`, `blocked_users`, `restaurants`, `post_reactions`, `post_comments`, `post_comment_likes`, `suppers`, `supper_members`, legacy `round_entries` |
| `/diary` | `napkin-app/app/diary.tsx` | `useUserDiary` | `user-profile?action=diary`; RPC `fn_user_diary_page` | `entries`, `entry_photos`, `profiles`, `restaurants` |
| `/reviews` | `napkin-app/app/reviews.tsx` | `useUserReviews` | `user-profile?action=reviews` | `entries`, `entry_photos`, `profiles`, `restaurants` |
| `/spots` | `napkin-app/app/spots.tsx` | `useUserSpots` | `user-profile?action=spots` | `entries`, `profiles`, `restaurants` |
| `/taste` | `napkin-app/app/taste.tsx` | `useUserTaste`, `useUserSpots` | `user-profile?action=taste/spots` | `entries`, `profiles`, `restaurants` |
| `/ledger` | `napkin-app/app/ledger.tsx` → `components/ledger/LedgerScreen.tsx` | `useLedger` (`users.ledger(viewer, month, tz, tableId?)`) | `user-profile?action=ledger`; RPC `fn_visible_entry_ids` | `follows`, `profiles`, `tables`, `table_members`, `entries`, `entry_tables`, `entry_companions`, `supper_members`, `blocked_users` |
| `/top-fours` | `napkin-app/app/top-fours.tsx` | `useTopFours` and top-four editor hooks | `top-fours` | `user_top_4`, `user_top_4_history`, `user_claimed_cities`, `user_claim_nudge`, `entries`, `entry_photos`, `profiles`, `restaurants` |
| `/follows` | `napkin-app/app/follows.tsx` | `useFollowList`; `FollowButton` → `useFollow` | `user-profile?action=followers/following/follow` | `profiles`, `follows`, `blocked_users` |
| `/member/[userId]` | `napkin-app/app/member/[userId].tsx` | member-profile query | `member-profile` | `profiles`, `entries`, `table_members`, legacy `table_nights` |
| `/u/[identifier]` | `napkin-app/app/u/[identifier].tsx` → `components/profile/ProfileScreenBody.tsx` | `useUserProfile`, `useUserSpots`, list hooks | `user-profile`, `lists` | `profiles`, `entries`, `entry_photos`, `follows`, `lists`, `list_entries`, `table_members`, `tables`, `user_top_4`, `user_profile_takes` |
| `/restaurant/[id]` | `napkin-app/app/restaurant/[id].tsx` + `components/restaurants/RestaurantPageV3.tsx`, `RestaurantOverview.tsx`, `RestaurantVisitActions.tsx`, `RestaurantRegularRow.tsx` | `useRestaurantPage`, `useReserveLink`, `useSimilarRestaurants`, lookup/clip/featured-list/list-membership/wishlist hooks | `restaurant-history?action=page/reserve_link/similar` + RPC `fn_visible_entry_ids`, `places-search`, `lists`, `wishlist` | `restaurants`, `entries`, `entry_tables`, `entry_photos`, `table_nights`, `table_night_participants`, `round_entries`, `profiles`, `blocked_users`, `tables`, `table_members`, `lists`, `list_entries`, `professional_critic_reviews`, `clip_thumbs`, `wishlist_items` |
| `/restaurant-history` | `napkin-app/app/restaurant-history.tsx` | `useRestaurantPage` (`restaurants.page`, preserving optional `tableId`), `useIsWishlisted`, `useMyLists`, `useListsContainingRestaurant`, `useRestaurantClippings` | `restaurant-history?action=page/social_clippings`, `wishlist?action=check`, `lists?action=list_mine/lists_containing` | `entries`, `entry_photos`, `table_nights`, `table_night_participants`, `round_entries`, `wishlist_items`, `lists`, `list_entries`, `clip_thumbs` |
| `/restaurant-reviews` | `napkin-app/app/restaurant-reviews.tsx` | `useRestaurantReviews` | `restaurant-history?action=reviews`; RPC `get_public_reviews_page` | `entries`, `entry_photos`, `profiles`, `restaurants` |

### Tables, Supper, Gather, and legacy rounds

| Route | Component file | Feeding hook(s) / client | EF / RPC | Backing tables |
|---|---|---|---|---|
| `/create-table` | `napkin-app/app/create-table.tsx` | `useCreateTable`, `useAddMember`, `useCreateInvite`, mutual-only user search | `table-management`, `user-profile` | `tables`, `table_members`, `table_invites`, `table_invitations`, `profiles`, `follows` |
| `/join-table` | `napkin-app/app/join-table.tsx` | `useJoinByInvite` | `table-management` | `table_invites`, `tables`, `table_members` |
| `/table/[id]/settings` | `napkin-app/app/table/[id]/settings.tsx` | `useTables`, member query, update/leave/delete hooks | `table-management` | `tables`, `table_members`, `table_invites`, `table_invitations` |
| `/table/[id]/atlas/[city]` | `napkin-app/app/table/[id]/atlas/[city].tsx` | table atlas and member hooks | `table-atlas` | `entries`, `entry_photos`, `profiles`, `restaurants`, `tables`, `table_members`, `suppers`, `supper_members`, legacy `table_nights` |
| `/looking-back` | `napkin-app/app/looking-back.tsx` | `useTableActivity`, `useTableMembers`, `useTables` | `table-activity`, `table-management` | `entries`, `entry_photos`, `profiles`, `restaurants`, `tables`, `table_members`, `suppers`, `gatherings`, legacy `table_nights` |
| `/seed-from-solo` | `napkin-app/app/seed-from-solo.tsx` | `useTables`, `useTableActivity`; local selection only | `table-management`, `table-activity` | `tables`, `table_members`, activity-side `entries`/`table_shares` |
| `/supper/[id]` | `napkin-app/app/supper/[id].tsx` | `useSupper`, add-take/delete helpers | `entry?action=supper-detail/add-take/delete-supper` | `suppers`, `supper_members`, `entries`, `entry_photos`, `profiles`, `restaurants`, `tables`, `table_members` |
| `/gathering/[id]` | `napkin-app/app/gathering/[id].tsx` | `useGathering`, `useRescueGathering` | `gatherings?action=get/rescue` | `gatherings`, `gathering_rsvps`, `profiles`, `restaurants`, `tables`, `table_members`, `wishlist_items`, `suppers`, `supper_members` |
| `/table-night` | `napkin-app/app/table-night.tsx` | `useTableNightStatus`, join/rate/ready/reveal hooks, Realtime presence | `table-night`; start/rate/reveal RPCs | legacy `table_nights`, `table_night_participants`, `round_entries`, `entries`, `restaurants`, `table_members` |
| `/table-night-detail` | `napkin-app/app/table-night-detail.tsx` | table-night status, direct entries/photos, history/interactions | `table-night`, `restaurant-history`, `post-interactions` | legacy `table_nights`, `round_entries`, `entries`, `entry_photos`, `profiles`, `restaurants`, reactions/comments |

Entry companion pickers list mutual follows only. Search uses `user-profile?action=search` with `mutual_only=true` and suppresses non-mutual rows; the recent row uses `user-profile?action=recent_companions`, which rechecks current mutual-follow and either-direction block state before hydration.

`/table-night` and `/table-night-detail` are retained code, not live product surfaces. The live group-meal model is Supper: `supper_id` clusters individual entries (`supabase/functions/entry/index.ts`, `hooks/suppers/index.ts`). Do not resurrect round UI when changing Tables.

- On a social Table with at least two current members, `TableLedgerModule` sits in Activity after `UpcomingStrip` and before invitations/feed content. Loading, query error, and an all-zero response are deliberately hidden with no spacer; populated state shows the first three rows and a trailing viewer rank when needed. The whole module opens `/ledger?tableId=` while leaving each trio member's summary available to accessibility readers.
- The Table header's map control pushes `/places-scope?scope=table&tableId=…`; hierarchical back returns to the same Table. Its Places ledger shows up to three overlapping 22pt member avatars and `+N` beyond three, with `pinned` relationship copy.
- `/create-table` uses the setup wordmark/back chrome, a paper field with an empty `Table name` placeholder, and an optional mutual-friend selection card. Selections say `Selected` and send pending invitations only after creation. `Create & share a link` explicitly creates the Table before minting its invitation URL. Empty/whitespace names refuse creation; the name field receives focus from the link action when empty. Pending locks both actions, inputs and back; failure permits retry; partial invitation/link failure preserves the created Table destination. A keyboard-avoiding footer keeps the primary action reachable.

### Lists, handoffs, notifications, and imports

| Route | Component file | Feeding hook(s) / client | EF / RPC | Backing tables |
|---|---|---|---|---|
| `/lists` | `napkin-app/app/lists.tsx` | `useMyLists`, `useSavedLists`, delete/save hooks | `lists` | `lists`, `list_entries`, `list_saves`, `profiles`, `tables`, `table_members` |
| `/list/new` | `napkin-app/app/list/new.tsx` | `useCreateList` | `lists?action=create` | `lists`, `table_members`, `tables` |
| `/list/[id]` | `napkin-app/app/list/[id].tsx` | `useList`, add/remove/reorder/note/save hooks, wishlist/handoff hooks | `lists`, `wishlist`, `handoff`; RPC `fn_add_list_entries_canonical`, `is_table_member` | `lists`, `list_entries`, `list_saves`, `wishlist_items`, `profiles`, `tables`, `table_members` |
| `/list/[id]/edit` | `napkin-app/app/list/[id]/edit.tsx` | `useList`, `useUpdateList`, `useDeleteList` | `lists` | `lists`, `list_entries`, `tables`, `table_members` |
| `/handoff` | `napkin-app/app/handoff.tsx` | handoff resolver and spot-save client | `handoff`, `resolve-url?action=save_spots` | `entries`, `list_entries`, `lists`, `profiles`, `wishlist_items`, `wishlist_shares` |
| `/share-detail` | `napkin-app/app/share-detail.tsx` | `usePostInteractions`, share removal client | `post-interactions`, `table-activity`/direct share removal path | `table_shares`, `entries`, `profiles`, reactions/comments, `table_members` |
| `/notifications` | `napkin-app/app/notifications.tsx` | `useNotifications`, `useUnreadCount`, mark-read and invitation-response hooks | `notifications`, `table-management` | `notifications`, `profiles`, `restaurants`, `table_invites`, `table_invitations`, `tables`, `entries`, `user_restaurant_status` |
| `/import` | `napkin-app/app/import.tsx` | local import queue/App Group bridge; `ImportLinkSheet` | `resolve-url`, `wishlist` save paths | local manifests plus `import_jobs`, `import_resolutions`, `restaurants`, `wishlist_items`, `table_shares`, `clip_thumbs` |
| `/import-review` | `napkin-app/app/import-review.tsx` | local manifest reader, `useMyLists`, `useTables`, import queue processor | `resolve-url`, `wishlist`, `lists`, `table-management` | local manifest plus `import_jobs`, `import_resolutions`, `wishlist_items`, `lists`, `list_entries`, `tables`, `table_members` |
| `/import-kickoff` | `napkin-app/app/import-kickoff.tsx` | large-job manifest and queue trigger | `resolve-url`, `wishlist`, `notifications` through `useProcessImportQueue` | local large-job state plus `import_jobs`, `import_resolutions`, `wishlist_items` |
| `/import-progress` | `napkin-app/app/import-progress.tsx` | active local manifests, root import reconciliation, recent import hooks, completeness hooks | `background-imports`, `wishlist`, `restaurant-completeness` | local manifests plus `background_import_jobs`, `import_jobs`, `list_imports`, `import_resolutions`, `restaurants` |
| `/import-digest` | `napkin-app/app/import-digest.tsx` | local digest manifest, correction/list/wishlist hooks | `places-search`, `restaurant-completeness`, `lists`, `wishlist` | local manifest plus `import_jobs`, `import_resolutions`, `restaurants`, `lists`, `list_entries`, `wishlist_items` |
| `/imports/[jobId]` | `napkin-app/app/imports/[jobId].tsx` | import-batch, exhausted completeness checks, correction/retry/dismiss, place persistence, repoint/remove/add-spot and list hooks | `wishlist`, `places-search`, `restaurant-completeness`, `lists` | `import_jobs`, `list_import_items`, `import_resolutions`, `restaurant_completeness_queue`, `restaurants`, `wishlist_items`, `lists`, `list_entries` |

### Settings and administration

| Route | Component file | Feeding hook(s) / client | EF / RPC | Backing tables |
|---|---|---|---|---|
| `/settings` | `napkin-app/app/settings/index.tsx` | `useUserProfile`, permission readers, account deletion client | `user-profile`, `account?action=delete-account` | `profiles`, `account_deletions`, `blocked_users`, `follows`, `table_members`, `tables`, user-owned image/data tables |
| `/settings/name` | `napkin-app/app/settings/name.tsx` | `useUserProfile`, `useUpdateProfile` | `user-profile` | `profiles` |
| `/settings/bio` | `napkin-app/app/settings/bio.tsx` | `useUserProfile`, `useUpdateProfile` | `user-profile` | `profiles` |
| `/settings/city` | `napkin-app/app/settings/city.tsx` | `useUserProfile`, `useUpdateProfile` | `user-profile` | `profiles` |
| `/settings/photo` | `napkin-app/app/settings/photo.tsx` | profile/photo update and image moderation helpers | `user-profile`, `moderate-image`; object upload | `profiles`, profile-image storage/staging |
| `/settings/username` | `napkin-app/app/settings/username.tsx` | availability check and profile update | `user-profile` | `profiles` |
| `/settings/privacy` | `napkin-app/app/settings/privacy.tsx` | `useUserProfile`, privacy/reply-permission mutations | `user-profile` | `profiles` |
| `/settings/privacy/make-public` | `napkin-app/app/settings/privacy/make-public.tsx` | username check/update and privacy mutation | `user-profile` | `profiles` |
| `/settings/blocked` | `napkin-app/app/settings/blocked.tsx` | blocked-list and unblock hooks | `account` | `blocked_users`, `profiles` |
| `/settings/import-tutorial` | `napkin-app/app/settings/import-tutorial.tsx` | local tutorial state | — | device-local state only |
| `/admin/critics` | `napkin-app/app/admin/critics.tsx` | `useIsAdmin`, critic-list/edit hooks | `critics-admin` | `admin_users`, `professional_critic_reviews`, `restaurants` |

### Data-shape rules that affect every route

- `entries` has no foreign key to `profiles`. Entry/profile surfaces batch-fetch profile IDs (`hooks/entries/useMySoloEntries.ts`, `supabase/functions/user-profile/index.ts`, `supabase/functions/restaurant-history/index.ts`); never add an ambiguous `profiles` embed.
- `table_members` identifies a person with `member_id`, not `user_id` (`supabase/functions/table-management/index.ts`, `hooks/tables/useTableMembers.ts`).
- Tables are private membership-scoped groups (`supabase/functions/table-activity/index.ts`, `supabase/functions/table-management/index.ts`). Profiles are public unless `profiles.account_privacy` makes them private (`supabase/functions/user-profile/index.ts`).
- Saves are public-by-default profile signals gated by `account_privacy`, read through SECURITY DEFINER RPC `fn_restaurant_saves_visible` (`supabase/functions/restaurant-history/index.ts`, `supabase/migrations/20260710160000_fn_restaurant_saves_visible.sql`).
- `visibility='private'` entries are hidden; public eligibility is centralized in `is_entry_publicly_eligible` (`supabase/functions/post-interactions/index.ts`, `app/entry-detail.tsx`).

### Places recency, restaurant context and notices (13 September 2026)

- In You → Pinned, `recently added` toggles newest-first ordering from `wishlist_items.created_at` (`placesScreenState.pinnedOrder`, `placesPresentation`, `PlacesScreen`). The map sheet and full paper list show saved date and city. Recent List uses chronological order across cities; unknown dates sort last with explicit unavailable copy. Existing map pins, facets and pagination remain. The auth-scoped choice survives map/list/search and is inactive for All, Been, Friends and Tables. No visit or restaurant creation dates substitute for pin dates.
- Restaurant Details uses `RestaurantLocationPreview` and existing restaurant/page/search coordinates. A 176pt neighborhood map appears only for valid latitude/longitude, with an exact venue pin and Open in Maps. Address/details remain when coordinates are absent. Search ghosts carry coordinates without persistence. Providers and attribution follow Places; no location permission is requested. Map/URL opening is read-only; failed URL opening shows retry guidance.
- On Socials (`OnSocialsRail`, `ClippingCard`) shows up to three source notes and an expand/collapse action for the fourth. Platform, creator when known, saver/date, and Open original remain visible without a thumbnail. A failed thumbnail becomes a platform glyph; a replacement URL retries. Only the existing authorized durable `social_clippings`/`clip_thumbs` projection supplies artwork. Video files without a URL and invalid URLs are informational, with no fake playback. Empty/visibility-filtered data hides the whole section; existing privacy and gone-source filtering are unchanged.
- `ActivityToast` presents one warm-paper notice at a time, with readable upright text, optional Imports icon/kicker, explicit action and independent 44pt dismissal. The entire action area activates the supplied callback once. A notice owns its lifetime: 4 seconds ordinary, 8 seconds actionable; actionable notices stay for screen-reader users. Pending notices receive their full visible lifetime and are cleared on owner changes.
- Import toast, local-notification and inbox actions use `importNoticeUrl` → `/import-progress?openJob=&outcome=&owner=`. The hub establishes the back stack and revalidates the current owned manifest before pushing a held review or completed large digest. Completed small imports use the server batch ID after local cleanup. Failed, consumed, absent local or wrong-owner targets stay at the hub; missing server IDs fall back there too. An initial OS notification waits for auth/onboarding to settle; existing Gather destinations continue to work. No import is approved or retried by opening a notice.
- Notifications inbox uses readable paper rows, upright proper names, visible import actions and generic source-accurate saved copy. Initial loading, filtered-page loading, intended-empty, initial failure/retry, retained data on refresh failure and pagination failures are distinct. Mark-read/invitation writes retain their existing hooks and refusal rules; live verification opens rows only through isolated fixtures to avoid mark-read writes.
- Verification: capture You → Pinned → recently added in map and List; toggle Been and return; open real restaurant Details read-only. Capture source notes with image, absent/failed image, unavailable/local-video, expanded/empty states, and notice/inbox loading/error/empty/content using local fixtures. Action routing, dismissal, owner changes, timer/queue behavior and all write paths use isolated tests. All external map/source links are read-only.

### Reviews and repeat visits (13 September 2026)

- `/restaurant/[id]` uses `RestaurantVisitActions`, `useRestaurantPage.self_log` and `useRestaurantVisitMutations`. A visit is one repeatable occasion. `Check in` creates a dated, unrated visit via `entry?action=record_visit`; intentional repeats use new nonces, uncertain retries reuse the original nonce. `Log a meal` opens the current native `/log-meal` composer and creates one visit with its review.
- The two main actions are permanently `Check in` then `Log a meal`; their labels, positions and purpose do not change with history, saving or selection. A separate `Your visits` row underneath shows the selected occasion’s date and either `Checked in` with `Add review`, or its actual rating/note/photo preview. `Add review` opens `/log-meal?entryId=…` for that exact eligible solo visit. Tapping a finished row opens `/entry-detail` read-only, with editing explicit there. The old `View your review` primary button and `New visit` detour are removed. History preserves every occasion, including same-day and older unreviewed visits; selecting one changes only the row content. Sorting uses creation time and ID; backdating never renumbers a visit.
- `useVisitReviewDraft` loads the owned entry plus photos, companion display rows and Table IDs. Every read must succeed before form hydration; no empty fallbacks can clear historic data. A cleared/unknown date remains null unless changed. Existing photos hydrate without re-upload. The obsolete `VisitReviewSheet` is removed.
- Existing-visit saves use `entry?action=save_visit` and the atomic `fn_save_visit(uuid,uuid,jsonb)` transaction, including rating/note/date/photos/liked/subratings and changed companion/Table selections. Omitted selections and existing visibility are preserved. The function uses the existing image lifecycle writers and rejects wrong owners, Supper/Round entries, unavailable Tables and invalid companions. No table schema, FK, RLS or public eligibility changes. `entry` must deploy with migration `20260913091351_visit_full_review_patch.sql` before the app.
- Confirmed saves patch the restaurant history and select the returned entry ID via an ephemeral owner/page-scoped query. The row shows actual saved review content (rating, two-line note and first-photo thumbnail) as persistent confirmation. Rating, words or photos qualify as reviewed. Empty review content returns to `Checked in`. Failed uploads block Save until retried/removed; failed visit saves retain the draft. New meal retries retain the submitted snapshot and nonce, avoiding duplicate visits after uncertain failures. Cancel makes no review mutation.
- The date sheet retains Today/Yesterday/Another day/No date. `Undo` is offered for the newly recorded bare visit; the server requires the latest owned, unshared, unenriched entry. A date alone remains metadata. Null dates retain existing calendar/merge behavior.
- States: the fixed main actions stay visible during loading and failures, disabled when writes are unsafe. An uncertain check-in disables new writes and exposes a separate `Retry check-in` affordance that reuses the original nonce. No visits hides the visit row. Selected bare visits offer Add review within their dated row; finished rows show content. Returned ID awaiting refresh never falls back to editing another visit. History/date sheets have explicit close controls. Draft loading/error/retry is visible before the composer; mutation failure keeps its values.
- Generic `/log-meal` always opens a new draft. `useAvailableVisitCheckIns` reads the existing owner-scoped restaurant history and suggests only solo, unreviewed, non-gathering check-ins. Using an existing check-in is explicit; the target date/time is visible and the selected owned draft is revalidated before hydration. Multiple candidates use a chooser. Switching a dirty draft requires discard/cancel; uploads are fenced and uncertain submissions cannot retarget. Suggestion loading/errors never block a new meal.
- `/restaurant-reviews` and public review photos/eligibility/pagination are unchanged. On-socials clip audience and appearance remain outside this approved visit-flow change.

| Control | Refusal conditions | Live verification |
|---|---|---|
| Check in / Retry check-in | Signed out, pending, absent identity; invalid/reused nonce for another restaurant | WRITE — fixtures only |
| Add review / Save review / date | Wrong owner, Supper/Round, invalid fields, photo failure, unavailable audience, pending or incomplete load | WRITE — opening/closing composer is read-only; saves use fixtures |
| Log a meal / Save meal | Pending, incomplete rating/photos; stale Table authorization; uncertain attempt must retry its snapshot | WRITE — composer open/close read-only; saves use fixtures |
| Undo | Latest owned bare visit only; no review, share, companion or gathering | DELETE — isolated SQL/component fixtures only |
| Open saved visit / history / choose visit / close | Missing target; disabled during writes; choosing a check-in revalidates owner and absence of a review | READ-ONLY — native simulator allowed; draft discard affects local state only |

## 3. Per-card states

“Intended-empty” below means the query completed and code received an empty array/zero/null allowed by its contract. “Broken-empty” means a query errored, never enabled, lost identity/parameters, or returned an unexpected shape. Preserve this distinction in screenshots and fixes.

### Launch screen (cold launch and the sign-in wait)

Source: `components/launch/LaunchProvider.tsx`, `LaunchScreen.tsx`, `launchState.ts`, `useLaunchSequence.ts`, `launchLayout.ts`; state reported by `providers/ConnectivityProvider.tsx` and `app/_layout.tsx::RootLayoutNav`; native splash from `app.config.ts` (`expo-splash-screen`) and `assets/images/splash-wordmark.png`, rendered from the Newsreader font by `scripts/brand/render-splash-wordmark.swift`.

- One screen replaces the former three (connection spinner, cold "No connection" page, "Checking your account…" gate). `OnboardingGateBoundary` and the connectivity gate now render plain paper underneath the cover and only report what they wait on.
- Native splash: the wordmark PNG (a 200pt square centred on the full screen) on paper `#fdf6ec` in light and dark mode. The launch screen draws the same PNG in the same square and hides the native splash two frames after the image loads (1.2 s fallback), so the handoff is invisible. Changing the splash needs a native build; a development client built before this change still shows the old terracotta tile.
- Intro (cold launch only): two terracotta rules draw outward from the wordmark (about 0.7 s) and dry to the masthead's quiet 25% hairline. Reduced motion fades the finished rules in instead.
- Loading: the cover stays until reachability is online, the session is restored, the onboarding gate has a real answer and the navigator has held one route for 120 ms. While waiting, a pen stroke runs along the lower rule (reduced motion: the rule breathes in place). After 3.5 s a quiet `Setting the table…` line appears; a normal launch shows no copy at all.
- Offline at cold launch: `No connection` with a `try again` pill (reachability refresh, busy while it runs); NetInfo also recovers on its own. Later drops keep using the in-app offline banner and never bring the cover back.
- Account unreachable: the onboarding-gate read exhausted its retries. The gate stays fail-closed; the cover shows `Couldn’t reach Napkin` with `try again` and retries by itself every 8 s while shown.
- Exit: the masthead lifts and fades, then the paper dissolves (about 0.4 s) into whichever route landed. Launch routing runs with stack animations off while the cover is up, so the `/` redirect and auth routing never show through.
- Sign-in wait: when signing in blocks the onboarding gate again, the cover returns without the intro (the masthead fades in) and leaves the same way. Token refreshes and metadata updates for the same person never re-block the gate, so the cover never interrupts normal use; a route change while the cover dissolves does not bring it back.
- Accessibility: while the cover is up it is modal for VoiceOver and the app underneath is hidden from TalkBack (`importantForAccessibility`); the wordmark is labelled `Napkin`; the lower rule is a busy progress bar while working; problem lines are alerts.
- Live safety: read-only. The `try again` controls only re-probe reachability or re-read `profiles.onboarded_at`.

### Feed and feed cards

Source: `app/(tabs)/feed.tsx`, `components/feed/FollowingFeed.tsx`, `components/feed/ForYouFeed.tsx`, `components/feed/FriendFeedCard.tsx`, `hooks/feed/useFriendsFeed.ts`, `hooks/feed/useSocials.ts`.

- Tabs are **Friends** and **For You**, in that order; Friends is the default on every Feed landing. The internal Friends mode key remains `following`. Switching tabs is local/read-only.
- Friends initial load with no cached rows: centered spinner.
- Friends initial failure with zero rows: explicit error state and retry.
- Friends later failure with cached rows: existing cards remain; do not call the retained list an empty success.
- Friends settled zero rows: invitation/switch-tab empty state. This is intended-empty only when `isError` is false.
- Friends content: one reverse chronological stream for you and the people you follow. Entries use `created_at` (visit date remains metadata), pins use `created_at`, personal lists use their latest `updated_at`. Own bylines say “you” in the same row anatomy. Rated entries say “tried”; notes say “noted”. Date kickers, terracotta tabs and paper-level spacing align with Places.
- Activity RPC gates every source before keyset/LIMIT. Self includes private/bare entries and private personal lists. Others require followed public accounts, no blocks in either direction, and existing public review eligibility / public list privacy. Pins must be undeleted and resolved. Table lists, Table context and raw pin notes/sources never appear. List rows represent latest state, not historical events.
- Two or more consecutive pins by the same person inside one date section fold into a single `pinned N places` digest row (`PinDigestRow`, `bookmarks-outline`, names as `A, B and N more`); tapping it unfolds the run into its individual pin rows in place for the rest of the session (local state, never persisted). A lone pin, a different actor, or a section boundary breaks the run. Pin rows open their restaurant; list rows open their list. Entry rows retain raw IDs for cache patching; non-entry IDs are namespaced. Activity uses a versioned cache beneath the friends prefix, separate from legacy entry pages/cursors. Older installed apps retain the original `fn_friends_feed` contract.
- Pagination adds a footer spinner; a sparse/end tail means no more eligible rows. Returning to Feed refetches. Only an explicit pull activates its refresh spinner; background/focus refetch does not move the content.
- For You independently loads social clips, people, and public lists. Any non-empty block renders even while a sibling block loads or fails.
- For You with no visible blocks and any active request: spinner. With any failed request: retry state. With all requests settled empty: “nothing here just yet” invitation.
- Friend-entry routing is literal and client-only (`feedWeight`): no prose + no photos → `ledger`; prose or one photo → paper-level `note` (one photo is a 42pt thumb); two or more photos, with or without prose → compressed `card`.
- Friends-feed rows carry no engagement controls. Compressed cards show only non-zero public like/reply counts; liking or replying requires opening entry detail. Tapping any weight opens entry detail, and long-pressing an owned entry opens owner actions.
- Tables activity cards may be entries, Suppers, Gathers, shares, floats, top-four changes, or list additions (`hooks/tables/useTableActivity.ts`, `components/tables/`). Round cards are legacy residue, not a new state to extend.

### New List composer

Source: `app/list/new.tsx`, `components/lists/CreateListSheet.tsx`, `ListComposeFields.tsx`, `useListComposer.ts`, `hooks/lists/useCreateList.ts`.

- Full screen and restaurant-inline creation share a muted Manrope `list name` placeholder; entered names use upright Newsreader. Both have an optional compact icon picker, always-visible note, ranked and public/private pills, and one rounded pinned Create list button. No standalone sorting hint appears below the pills; only Table lists show the audience explanation. Counters appear near their limits (name 50/60, note 120/140). Existing list editing keeps its default emoji picker.
- Table context replaces personal visibility with the named Table audience; submission forces private and carries `table_id`. Personal lists default public. Inline creation preserves its seeded restaurant payload/ID and starts a fresh draft each presentation.
- Empty name, missing user and pending submission disable create. A synchronous ref guards double presses; pending work freezes fields and cancellation. Failure keeps the draft with an inline retry explanation; stale completion after dismissal/account change cannot navigate. Keyboard Done dismisses the keyboard; Create submits.
- Full-screen keyboard avoidance and inline keyboard-height padding keep the pinned action visible while fields scroll on short screens. Icon, ranking, audience and cancellation controls have 44pt targets.
- Live safety: opening/dismissing and changing the local draft are read-only. **Create list is LIVE-WRITE** (`lists?action=create`, optionally an initial list entry); test submit/retry/pending/seed/Table payloads with mocks, never against production.

### Table tab masthead, segments, and map doorway

Source: `app/(tabs)/tables.tsx`, `components/tables/TableHeader.tsx`, `components/tables/TableSegments.tsx`, `components/tables/OnTheMapRow.tsx`, `hooks/tables/useTables.ts`.

- Masthead: upright `Newsreader_600SemiBold` 28/34 table name + switcher caret, bell, and settings gear (social tables only). There is no `TABLE` kicker and no `map` chip — both retired by TICKET-238.
- Members row: avatar stack (max 4, then `+N`), `N members`, and a ghost `invite` chip that renders only when the viewer owns the table (`add_member` is owner-gated server-side).
- Segments are `activity · lists` (the `activity`-only branch keys off `Table.is_personal`, a legacy field the server no longer sends — every live table is social); `atlas` appears only when `FRIEND_TEST.hideAtlas` is off. There is no Wishlist segment: the Table's saved places live in Places under the table scope. Switching segments is local/read-only, and a segment the active table cannot show clamps back to `activity`.
- The `on the map` row sits under the segments on the Activity pane and pushes `/places-scope?scope=table&tableId=…`; back returns to the Table tab. It is deliberately count-free — a meta line would mount `useTableWishlist` and `useTableMapPins` on a pane that renders neither. Personal tables get no doorway.

### Journal

Source: `app/(tabs)/journal.tsx`, `components/journal/JournalList.tsx`, `hooks/entries/useMySoloEntries.ts`.

- Initial load: spinner. Failure: explicit retry/error state. Success with zero entries: empty journal CTA. Success with rows: month-grouped journal cards.
- The historical hook name `useMySoloEntries` is misleading: RPC `fn_my_solo_entries` now returns **all entries authored by the user, including table-shared entries**. There is no Table filter.
- Intended empty is a successful zero-row RPC response. Broken empty is `isError`, absent authenticated user ID, or a profile/restaurant batch hydration failure; the route has an explicit query error branch.

### Entry detail

Source: `app/entry-detail.tsx`, `lib/screenLoadState.ts`.

- A genuinely pending entry or night-to-entry resolution shows the centered spinner. Cached entry data remains readable during a failed refetch.
- A failed entry read, failed `resolveEntryIdByNight`, or a settled route with no resolved entry shows “could not load this entry.” The back control is always present; retry appears when the route has enough identity to replay the request.
- Loaded entry content retains its eligibility, privacy, Supper, legacy Round, interaction, and editing states. A missing required entry is broken-empty, never loading or intended-empty.

### Restaurant page sections

Source: `app/restaurant/[id].tsx`, `app/restaurant-history.tsx`, `components/restaurants/RestaurantOverview.tsx`, `components/restaurants/RestaurantVisitActions.tsx`, `components/restaurants/MemoriesStrip.tsx`, `components/restaurants/RestaurantHistoryRows.tsx`, `components/restaurants/RestaurantPageV3.tsx`, `components/restaurants/RestaurantRegularRow.tsx`, `hooks/restaurants/useRestaurantPage.ts`, `hooks/restaurants/useRestaurantClippings.ts`, `lib/restaurantPhoto.ts`.

- A persisted restaurant starts with a loading treatment. A Places payload/lookup can synthesize a ghost identity before a canonical row exists. Failure before identity exists shows the escaped “could not load this restaurant” shell with back + retry; a resolved `restaurant:null` is broken-empty, never blank paper.
- Cold success chooses one of two mastheads. Photo mode is a full-bleed pager with a compact default of 320pt or 40% of the window, whichever is smaller, with safe-area back/pin fabs, name/meta over the photo, and paper overlapping its lower edge. Measured title and photo-credit heights expand only a crowded masthead to preserve their separation for long names or larger text. The actual rendered photo height controls when status glyphs turn dark over paper. No-photo success retains the prior plain top bar + upright typographic masthead without a spacer or alternate invitation copy.
- `resolveMastheadPhotos(page, clippings)` is the detail-page owner for the chain attributed Places hero FIRST → entry photos as later pages → none (founder order 2026-09-06: a user's review photo never becomes the face of a restaurant, and a clipping thumbnail never enters the masthead). Frame one is always the venue's Google photo; without one the masthead stays typographic even when entry photos exist (they live in the memories strip and review cards instead). Entry photos come from `self_log[].photos`, `photos.from_your_table`, `photos.from_others`, and `public_reviews[].photo_url`; they dedupe against the hero, cap the pager at four, reset to page one when the set changes, and open the backing entry when its id is present. The clipping argument is accepted for call-site compatibility and ignored. The Places frame's chip reads `via google · 1 / N` whenever later pages exist, so the pager is discoverable. Places mode requires `photo_source='places'`, uses the stored/proxied `photo_url`, and reuses the shared fail-closed Places credit parser; a credit matching the restaurant name is suppressed as redundant. The Places frame is inert.
- `RestaurantOverview` follows the masthead with one compact paper-level Napkin line: star, average, rating count and a separate written-review doorway. The average and rating count come only from the server's visibility-gated `napkin_aggregate.average/count`, never the truncated `public_reviews` preview. `public_reviews_total` supplies the written-review count only. A valid average requires a finite value from 0.5 to 5 and a positive safe-integer rating count. Written reviews require a positive safe-integer count; the line opens `/restaurant-reviews` only when a callback is supplied. An omitted aggregate with existing written reviews shows just the review count, not a fabricated average. No aggregate or reviews reads `No reviews yet`; loading and unavailable data read `Loading reviews…` and `Reviews unavailable` separately. Google and practical links remain in Details lower on the page. The restaurant and Reviews screens apply system status glyphs only while focused: Reviews uses dark glyphs on paper, and returning restores the restaurant’s photo-aware glyphs.
- Rating spread (`buildRestaurantSpread`) paints the followee cohort when three or more friends rated (`friends land on`), otherwise the server's visibility-gated `distributions_half.napkin` bins when they hold two or more ratings (`ratings land on`, since those bins count entries, the viewer's included, not distinct people); it is intended-empty only when both rings are thinner than that.
- The memories strip follows the actions for persisted restaurants only. It keeps its authorized source order and first-12 cap, removes duplicate URLs, and also excludes every entry photo already promoted into the masthead. Ghosts, zero remaining authorized photos, and legacy payloads without the `photos` projection render no section or spacer. Entry-backed tiles open entry detail; projection rows without an entry id stay inert.
- The action rail keeps two permanent buttons (`Check in`, `Log a meal`) followed by the dated personal visit row and compact regular status. Reading or selecting a visit never replaces those buttons. Pinning remains the masthead bookmark. Details contains the map, address/directions, hours, phone, website, `reserve a table` (only after `useReserveLink` resolves a real booking page), `gather the table` (Table membership plus a persisted restaurant), and the faint Google rating/count row. Those external facts and actions do not appear in the top Napkin summary.
- The reviews section previews up to three public review cards (`previewReviews`: followee cards first, then other public reviews, never the viewer's own, deduped by entry); each note clips at three lines, the review's photos render inline beneath it through the shared `ReviewPhotoStrip` (1–3 tiles, `+N` overflow on the third, taps open the read-only `PhotoLightbox`), and the `all N reviews ›` doorway below opens `/restaurant-reviews`. The heading reads `Friends’ reviews` only when every previewed card is a followee's, otherwise `Reviews`. With a public total but no previewable card it collapses to the single `Reviews · all N reviews` doorway row. The `From <Table name>` section renders at most two rows from one authorized `table_notes` share-edge group and opens `/(tabs)/tables?selected=&section=activity`. Table-note authors must be current members of that exact shared Table; the rings never cross-feed.
- When an older server omits `self_log`, its compatible `personal.visit_count` remains available to history; it does not substitute for Napkin's rating or written-review counts. Warm scoped and unscoped arrivals reuse the exact `restaurants.page(id, tableId?)` cache; a cold `/restaurant-history?id=` deep link shows loading, then the ledger, and its back control replaces to the restaurant when no parent exists. Query error without cache is broken-empty (`ErrorState` + retry); a warm refetch error keeps cached content and shows a one-line retry state on both screens.
- The regular (`RestaurantRegularRow`: compact avatar, upright name with inline `The regular`, `N visits · <runner-up> is N behind` / `tied with`) sits within `RestaurantVisitActions` after the personal visit receipt, before memories. It has no standalone section heading and is not inside the self-history gate: a followee can hold the crown when the viewer has no visit here. Since 2026-09-06 the restaurant-page crown is ALL TIME over rated meals, dated or not (`loadRestaurantRegular`, read category `regular`, minimum two visits; the monthly ledger crown keeps its own rolling-90-day window). LAW: `regular` KEEPS the rating filter. A silent check-in by anyone but the viewer cannot survive `fn_visible_entry_ids` (a bare entry has no table, companion or supper edge, so its only branch is the public-account one, which requires a rating), so counting unrated rows would admit the viewer's own while dropping every followee's and hand the viewer a self-flattering crown. A tie reads `tied with <name>` in both the visible line and the screen-reader label. It is hidden when `regular_detail` is null; crown-read failures are non-fatal and suppress only this row, never the core restaurant page. Cohort = viewer + followees only, filtered before the shared visibility RPC, so public strangers and unauthorized private logs never hold or affect the crown while authorized Table/companion/Supper rows can. A followee's row opens `/u/[identifier]`; the viewer's own crown (`you`) is inert.
- `/restaurant-history` renders every viewer-authored visit as a paper-level row, including unrated, non-host, revealed, and closed legacy takes. Rows keep full notes, optional companions, and 96pt photo layouts (one/two squares or a three-up fill with `+N` on overflow), separated only by `dividerSoft`; there is no card fill or shadow. A resolved empty `self_log` is an intended empty invitation; an omitted legacy-server projection shows the degraded inline retry state and never fabricates an empty ledger. `table_notes` follows the same optional-projection rule, so omission hides the section. `ELSEWHERE` is intended-empty unless a pin, containing list, or self clipping exists. It is read-only: row navigation and list doorways mutate nothing.
- Similar places (`SimilarPlacesSection`, the last section on the page, below Details) carousels up to six other `restaurants` rows from the same city via `restaurant-history?action=similar` (`useSimilarRestaurants`, 30-minute stale time): cuisine match first, then a specific shared place type, each tier by distance, within 5 km, self and merged aliases excluded, unverified import ghosts only for their creator. A candidate matching neither is dropped: proximity alone is not similarity, so a venue whose own cuisine and place types are all generic (Google gave it nothing specific) returns no rows and the section is intended-empty. Cards scroll horizontally: a photo thumbnail on the restaurant's plate tint, then name in upright Newsreader and `cuisine · 0.3 km`, and open that restaurant; The heading always reads `Similar places`. Thumbnails resolve through `resolveSourcedPhoto`, so a Places photo whose attribution will not parse fails closed to the tint plate with a `restaurant-outline` glyph, and the surviving non-redundant authors are named once under the strip (`photos via …`) rather than on every card. It reads only place facts, never entries, pins or profiles, so it carries no user signal. Intended-empty (no section) when the source has no city or coordinates, or when nothing qualifies; a failed request also hides it. Featured lists, social clips, Table atlas context and hours remain conditional on real data. Details makes an `open until` claim only when `place_details.open_now === true`; otherwise it uses the weekday line. Google remains its last faint fact, `N.N on google` plus a rating count only when positive; it never contributes to the Napkin average. Page subsection titles use the same 17pt upright Manrope token, `Type.restaurantSectionTitle`, in sentence case: Your visits, Reviews/Friends’ reviews, the chosen Table name, Rating spread, In lists, Details, Similar places and On socials. On socials keeps its source-note rail behavior. The Tables `section=activity` arrival is consumed once per stable navigation params object; later pane/Table changes stay selected, while a new params object re-applies and omitting `section` preserves the current pane.

- `restaurant-history?action=user_history` reports `last_visit` as the most recent RATED visit (`ratedVisits[0]`), falling back to the newest entry only when none is rated. `visit_count` still counts every entry. This keeps entry-detail's `PreviouslyHereBanner` naming a date it can also show a rating for; since check-ins carry today's date, `visits[0]` would otherwise be a silent check-in and the banner's rating would silently vanish. `action=page` already used the rated set, and the two surfaces must agree.
- The monthly ledger awards a new place only for a (member, restaurant) pair that has a visible RATED meal that month, matching `meals` and `crowns`. The lookback still contributes unrated and undated rows, so an earlier silent check-in still proves a prior visit and blocks a false first-visit claim. Without that pair gate a silent check-in scored a napkin with `meals = 0`, and only when a followee happened to rate the same restaurant that month, because the lookback restaurant set is built from the whole cohort chunk's month rows.
- `scripts/test-visits-native.sh` applies every migration mentioning a visit RPC in filename order rather than a hardcoded pair, so the harness can never test a stale definition after a later migration redefines one. That script is the only invocation of `supabase/tests/visits.concurrency.sh` in the repo.

### Profile sections

Source: `components/profile/ProfileScreenBody.tsx`, `components/profile/ProfileNapkinsLine.tsx`, `hooks/users/useUserProfile.ts`, `hooks/users/useUserSpots.ts`, `hooks/users/useLedger.ts`, `supabase/functions/user-profile/index.ts`.

- Missing route identity or initial load: spinner. Query error: “Couldn't load”. `isNotFound`: not-found page.
- `blocked_by_viewer`: blocked-user stub with an unblock doorway. This is not a missing profile.
- `private_stub`: identity, relationship controls, and allowed counts remain; journal/palate content is withheld with a private-account message.
- Normal self/public profile: identity header plus conditional palate, top-four, quick-take, list, activity, and spot sections.
- The monthly napkins line is rendered and fetched only for `isSelf && inTab`; its cache key uses the authenticated viewer id. `/u/[identifier]` neither paints nor enables the ledger query. It opens the friends-scoped `/ledger` one level down from Profile.
- Self with zero logs: cold-start nudge. Empty top fours, quick takes, lists, or imports are omitted by their array/gate logic; imports are self-only and appear when an action is owed.
- Intended empty is an explicit server state (`private_stub`, `blocked_by_viewer`, `isNotFound`) or successful empty section array. Broken empty is `isError`, an unresolved identity, or a section request that failed while the main profile stayed cached.

### The ledger

Source: `app/ledger.tsx`, `components/ledger/LedgerScreen.tsx`, `hooks/users/useLedger.ts`, `supabase/functions/_shared/ledger.ts`, `supabase/functions/user-profile/index.ts`.

- Friends scope is the viewer plus their 500 most-recently-followed accounts (maximum cohort 501). This implementation cap is intentionally not on-screen copy.
- Table scope is the 500 most-recent current members by `joined_at`; it is entered only from a Table doorway. Membership is checked after month/time-zone validation and before candidate reads; non-members receive structured `NOT_A_MEMBER` 403. Private solo entries remain invisible while entries shared through an authorized Table/companion/Supper scope can survive the same gate.
- Month and IANA time zone are required. The server derives half-open local-month bounds; past months snapshot at month end, the current month ends at now, and future months return structured `FUTURE_MONTH` 400.
- Every meal, crown-window, and new-place lookback read uses separate paginated `visited_at` and null-`visited_at`/`created_at` branches. Cohort filtering happens before one deduplicated visibility pass over all non-self candidates; self rows bypass RPC. Meals, visible-first new places, and crowns are aggregated only after that gate.
- The masthead kicker identifies only `FRIENDS` or the Table name; the picker owns the single month label and there is no scope toggle. The month arrows move one calendar month, refresh their future gate when the route regains focus, and use 44pt hit targets. A friends ring with no followees shows `follow a few friends and the ledger fills itself`; loading and error remain distinct states. Rows are read-only, ranked, virtualized through `FlatList`, and the viewer row alone uses `primaryMuted`.

### One Places map and its scopes

Source: `app/(tabs)/places.tsx`, `app/places-scope.tsx`, `components/places/PlacesScreen.tsx`, `components/places/ScopePickerSheet.tsx`, `components/places/TableScopeRow.tsx`, `components/wishlist/WishlistMapView.tsx`, `hooks/search/placesScreenState.ts`, `hooks/wishlist/useMyWishlist.ts`, `hooks/users/useUserSpots.ts`, `hooks/users/useNetworkMapPins.ts`, `hooks/wishlist/useTableWishlist.ts`, `hooks/tables/useTableMapPins.ts`, `hooks/tables/useTableMembers.ts`.

Places is the sole map workspace. The tab owns a `you` / `friends` / Table who scope; Table doorways push the locked `/places-scope` instance. Pinned-list legacy destinations now land on `/(tabs)/places?view=list&layer=pinned`.

- `you` uses personal pinned/been data. `friends` alone arms `network_map_pins`. A Table scope shows its overlap rows for `all`/`pinned` and its gathered rows only for `been`; the two Table sources never merge, so olive pins do not appear under Table `all`/`pinned`.
- The who chip is immediately after locality. The active Table alone resolves up to two member faces; opening the picker unmounts that member query. Picker metadata is cache-only and unknown counts are omitted.
- Table rows use upright restaurant names, `cuisine · city · N of you pinned` or `<first name> pinned`, member avatars and a chevron; gathered rows say `gathered <date>`. The amber overlap numeral is upright.
- Zooming out does **not** cluster pins (`WishlistMapView` renders all pins directly). Location is never prompted until a user invokes a location affordance.

### Handoff

Source: `app/handoff.tsx`, `hooks/wishlist/useResolveHandoff.ts`, `lib/handoffNavigation.ts`.

- Loading shows warm paper; a live share shows candidate rows and the pin action; revoked, invalid, missing-token, and save-revoked states show the existing tombstone.
- Every back/close path and successful pin dismisses one level when a parent stack exists. A cold deep link with no parent replaces to the Places pinned-list route, so no exit is a no-op.
- Failed individual saves retain the candidate rows for retry. A terminal share error is not an empty successful handoff.

### Settings privacy

Source: `app/settings/privacy.tsx`, `hooks/users/useUserProfile.ts`, `lib/screenLoadState.ts`.

- Initial profile load shows the centered spinner. Cached profile data remains readable during a failed refetch.
- A failed or settled-missing profile shows “could not load visibility” with back and retry; the hidden navigator header never leaves this state without an escape control.
- A loaded profile renders public/private mode and reply controls. Missing profile data is broken-empty, not an indefinitely loading privacy state.

### Imports

Source: `app/import.tsx`, `app/import-review.tsx`, `app/import-kickoff.tsx`, `app/import-progress.tsx`, `app/import-digest.tsx`, `app/imports/[jobId].tsx`, `hooks/wishlist/useProcessImportQueue.ts`, `lib/importFastPath.ts`, `lib/largeImportJob.ts`.

- TikTok photo posts preserve the actual `imagePost.title` separately from the caption (`lib/tiktokPerception.ts`, `lib/photoImportFusion.ts`). Queued shares fuse it before slide OCR, including failed downloads/empty OCR; pasted links resolve title/caption directly without slide downloads (`lib/resolveTikTokVideo.ts`). Unknown slide counts are omitted. Successful empty photo evidence does not retry generic oEmbed; cancellation/provider failure retains existing failure behavior. No import or save is performed against production during verification; transport and write paths use fixtures.

- Queue/manifest phases include reading, saving, review, kickoff, completed/digest, and failed/poisoned. The hub may retain history after active work ends.
- `/import-progress` with active manifests shows per-import progress; with recent history shows completed/failed cards; with neither shows education/empty hub.
- `/import-review` with candidates shows approve/exclude/destination controls. Missing manifest or zero spots produces “nothing to review”; inspect local manifest presence to distinguish expired/broken handoff from an intentional zero-candidate extraction.
- `/import-digest` shows saved/rejected/repair rows. Missing job/manifest and “nothing left” are distinct code paths.
- Import candidate “not this?” correction starts from the bare extracted name and sends structured city/area plus granted-only device coordinates to `places-search`; it never duplicates `best_query` locality.
- `/imports/[jobId]` (Review clip) reads exhausted completeness items for the current job through `ImportChecks`. Each flagged place appears once, before ordinary saved cards, with a cause-specific explanation (name, location, ambiguous, no result, type, or safe fallback). Visible actions are `find correct place`, `try matching again` for retryable/legacy checks, and `keep as saved` (only for a surviving pin) or `dismiss check`. Keep/dismiss clears the check without deleting saves. Find mints fresh owner-bound provenance before correcting; correct/retry only queue work, so their feedback describes background matching rather than claiming verified completion. Pending feedback offers `refresh places`, and pull-to-refresh reloads both batch identities and checks so a completed worker update becomes visible without leaving the page. Refresh progress appears only after an explicit refresh; background polling never activates the pull spinner. Busy actions lock synchronously, retain the active card through optimistic removal, and keep failures inline. Failed, loading, or paginated check reads never claim all clear. More checks remain reachable through pagination. Saved cards label `view place`, `change place`, `add to list`, and `remove pin`. The check picker prefills the imported town; change-place uses the currently saved town. Both towns are editable. The picker debounces searches, cancels stale/closed requests, and separates search errors from no results. These are LIVE-UNSAFE writes (correction, retry, dismiss, change, remove, list/add), tested only with isolated fixtures; opening, typing search, paging, and dismissing the picker are read-only.
- `/imports/[jobId]` shows load, failed-load with retry, not-found, populated batch, and no-saved-places states. Error/not-found is not an empty successful batch. Fix/add picks persist a ghost Place before the wishlist write; a failed pick stays open with its error inline in the page sheet, while success dismisses before the toast. Malformed wishlist ids are rejected with a 400 instead of surfacing as a 500.
- Review clip keeps `open TikTok`, `open Instagram`, or `open source` above check cards and saved places, including checks-only and empty batches. This read-only external link preserves the review route and uses validated HTTP(S) source URLs; uploaded videos and absent/invalid links have no action. Opening failures show a toast. Legacy jobs with no source recover one unambiguous original from owner/job-scoped completeness sources, then saved items; conflicting or truncated source samples stay hidden. Only source identity is returned, never internal client facts. Google Maps imports use `open Google Maps`.
- Video import runs OCR/perception on device before server resolution (`useProcessImportQueue`). TikTok's cheap fast path requires a single high-confidence candidate whose distinctive name is present in the caption (`lib/importFastPath.ts`); caption-free, uncorroborated and multi-candidate input escalates. Pasted TikTok links use the same gate and bounded download/OCR ladder (`lib/resolveTikTokVideo.ts`) in the existing loading sheet. Dismissal or replacement suppresses stale results, cancels downloads and cleans temporary video after native work settles. Shared, pasted and picked videos preserve separate speech/on-screen evidence and prioritize the final eight seconds, including the last second; empty evidence retains the existing failure/recovery states.
- Clip tray video and screenshot actions open the matching native Photos picker directly. `ImportLinkSheet` presents an empty transparent host without animation or another scrim, waits for its native `onShow`, then opens Photos; the import-source menu never paints during this handoff. Cancel returns to the tray, and reopening starts a fresh picker. With native API 5, video selection persists an owner-bound review-mode manifest and closes Photos, the import host, and the tray before native download/copy finishes. `sourcePreparation:'pending'` appears as `downloading video` and never enters OCR, saving, or destination edits. The native prepared event only pokes the durable queue. Ready files progress through root-owned OCR and matching while the user continues using Napkin. Older native binaries first prepare an owned Documents copy, then enqueue the same review-mode work. Screenshot selection keeps its existing upload/destination flow. Picker/capture failures show retry and never claim queued success. Opening and cancelling Photos is read-only; selecting media, resolving, or saving stays fixture-only during verification.
- Share-time wake (TICKET-248): after the share card writes its review-mode manifest, `BackgroundImportTransfer.wake` starts a tiny background URLSession upload (not before three seconds, `earliestBeginDate`) to `background-imports?action=wake` with the scoped intake credential (`backgroundImportIntake.ts`, root `useBackgroundImportIntake`). The server answers eight seconds later so the extension is gone first; then iOS launches or resumes Napkin in the background (`BackgroundImportAppDelegateSubscriber`); `ImportWakeRuntime` holds background time until the JS drain takes its own (also when a finished import download wakes the app, so OCR can run), and the drain processes the share on the phone. The card says `Napkin is getting the spots ready` when a wake started, `open Napkin when you're ready to check the spots` without a credential. There is no app switch and no server job. iOS allows about 30 s per wake and never relaunches a force-quit app; paused video work resumes the next time Napkin runs. `patches/expo-file-system+19.0.22.patch` releases session completion handlers that expo-file-system does not own; without it UIKit is never told the wake finished.
- Manifests from builds 262+ that carry `remoteJobId` process locally at once and dismiss their server copy (housekeeping retries until the server acknowledges, `remoteDismissed`); nothing waits on the server and the five-second remote polling is gone. Server housekeeping for retired server-lane jobs runs after local work. A server-answered 5xx (extraction timeout, provider error) retries once after ten seconds, then the import fails visibly with try-again (`serverFailures`, `MAX_SERVER_FAILURES`); 429 and network loss keep resume-on-next-drain. Finished on-device perception that read new text (OCR, speech or a recovered page channel) is checkpointed on the manifest (`evidence`) before the server resolve, so a wake that iOS cuts short or a server failure resumes at matching without another download/read; spots or a manual try-again clear it. These are LIVE-UNSAFE writes exercised with isolated fixtures.
- `background-imports` serves owner-JWT status/list/cancel, intake-only URL enqueue and (TICKET-248) an intake-only `wake` that authenticates and stores nothing; private `background_import_credentials` / `background_import_jobs` back the queue. Since TICKET-248 an enqueued job is handed to the device at once (`needs_device`, reason `device_owns_import`) instead of kicking the worker: the worker cannot read TikTok/Instagram media, and a pending job made installed builds wait on it for hours. The worker itself now only hands claimed jobs to their device (no paid extraction, never a server `ready`), so a late "import ready" can never arrive for a job the phone handled. Every response to an extension's `wake`/`enqueue` upload, failures included, is held for `EXTENSION_WAKE_DELAY_MS` (8 s): iOS relaunches the app for a share's background upload only if the extension is gone when it completes, and a fast reply lands back in the still-open extension instead (Apple DTS forums 76659). App (JWT) calls are never delayed. Import completion adds an idempotent inbox event and Expo push using `import_push_devices` / `import_push_deliveries`. A durable producer checkpoint prevents recovery starving newer jobs. Push requests permission at the existing import moment and registers on launch when permission is already granted. Registration uses cryptographic installation proof, session ownership and bounded sign-out cleanup; revoked permissions or account changes remove the destination. Notification taps check owner before entering the hub. Push tickets/receipts do not establish physical device delivery.
- Gallery acceptance shows queued feedback with a `view` action after the tray closes and offers the existing notification-permission prompt at that safe point. Ready results stay unsaved in `/import-review`, reached through the tray or `/import-progress`; completion uses a foreground toast, or a device-local notification when backgrounded and permitted, plus an owner-fenced durable inbox event. New notification checkpoints replay interrupted delivery without reannouncing legacy held jobs; a separate local-attempt checkpoint prevents inbox retries from repeating the toast/banner. Pending imports run before failure-notification replay; replay rotates through at most three rows per drain, each inbox request has a five-second deadline, and another enqueue takes priority before the next replay. Native preparation failure or a source missing after relaunch remains a failed row with `choose video again` on API 5. Older binaries show `update Napkin to choose this video again` and keep discard available without a nonfunctional picker action. Cancel/error retains the old row; a replacement removes it only after another owner-bound capture is verified durable. Other extraction/matching failures preserve the owned source for normal retry/discard. UIKit background time is bounded: suspension or termination is not a promise of uninterrupted on-device processing; unfinished work resumes or exposes recovery on next open.
- Maps lists over 20 return `mode:'large_list'`, use a client-pumped background job in chunks of 20, and post a local completion notification when backgrounded (`lib/largeImportJob.ts`, `useProcessImportQueue`).
- Google Maps share links resolve in `resolve-url` by walking redirects one hop at a time and stopping at the first URL that names the place (`resolve-url/mapsList.ts::expandMapsShare`). App-made links name it in hop one as `?q=Name, full address`; web-made links as `/maps/place/<name>/…!3d<lat>!4d<lng>`. The Places search is then located by the link itself: the place pin as a coordinate bias, or `use_home_city:false` for a self-addressed query, never the sharer's home city (`mapsPlaceSearchLocality`, `places-search/utils.ts::buildTextSearchPlan`). Only Google hosts are followed; a consent interstitial is unwrapped from its `continue` parameter; lists still load the page for the getlist preload.
- A long “reading” state can be active local OCR/background work, not a dead server request. Confirm the manifest phase/timestamps before declaring it stuck.

### Supper and Gather

Source: `components/suppers/SupperCard.tsx`, `app/supper/[id].tsx`, `components/gatherings/GatheringCard.tsx`, `components/gatherings/GatherSheet.tsx`, `app/gathering/[id].tsx`, `hooks/suppers/index.ts`, `hooks/gatherings/index.ts`.

- Supper cards distinguish empty, filling with viewer seat empty, filling with viewer take present, and gathered. Pending members may appear as ghost seats.
- Supper detail: loading; populated roster/takes; or “not available” for missing/error/unauthorized data. Host-only destructive controls and viewer add-take doorways are conditional.
- An empty viewer seat is intended when the authenticated member has no attached entry. A failed supper query uses the unavailable/error branch, not an empty seat.
- Gather lifecycle states include proposed, dispatched, expired, rescued, and cancelled. Viewer RSVP can be unanswered, in, out, or countered.
- Host affordances include reschedule/cancel/clear/rescue under state gates. `GONE_CODES` are terminal server states; a cache seed may remain briefly while a detail refetch resolves.
- Rescue is host-only for an expired, unrescued Gather and opens a nested table-selection flow. A transient fetch error is not an expired Gather.

### Places (supersedes the Search tab and nav Map item)

Source: `app/(tabs)/places.tsx`, `app/places-scope.tsx`, `components/places/PlacesScreen.tsx`, `components/places/ScopePickerSheet.tsx`, `components/places/TableScopeRow.tsx`, `components/wishlist/WishlistMapView.tsx`, `components/wishlist/ClipTray.tsx`, `components/wishlist/clipTrayUtils.ts`, `components/sheets/SnapSheet.tsx`, `components/places/PlacesRow.tsx`, `components/places/PlacesSelectedCard.tsx`, `components/places/PlacesSearchSections.tsx`, `components/places/PlacesListsPane.tsx`, `components/places/placesPresentation.ts`, `components/search/SearchLocalityBar.tsx`, `components/search/PeopleSearchPane.tsx`, `components/search/ListsSearchPane.tsx`, `components/search/RecentSearchesList.tsx`, `components/search/ListRow.tsx`, `hooks/lists/useMyLists.ts`, `hooks/lists/useSavedLists.ts`, `hooks/users/useFollowingList.ts`, `hooks/users/useNetworkMapPins.ts`, `hooks/wishlist/useTableWishlist.ts`, `hooks/tables/useTableMapPins.ts`, `hooks/tables/useTableMembers.ts`, `hooks/search/useRestaurantSearch.ts`, `hooks/search/placesScreenState.ts`, `hooks/search/useSearchLocality.ts`, `providers/AuthProvider.tsx`.

- **Browse header / nearby:** `places · lists · people` is always present above the ordinary sheet shelf. Places defaults to the `you` scope and the deduped union of pinned and been restaurants; overlaps render once with the been marker. The who chip selects `you`, `friends`, or one of the viewer's own Tables; pinned and been remain mutually exclusive layer filters, and tapping the active layer releases to `all`. Valid coordinates permit nearest-first order and distance tokens; denied/no coordinates preserve source order and omit distance. A cold active-source failure renders retryable broken-empty; a refetch failure with cached rows preserves those rows and adds compact inline retry.
- **Who picker / cost gates:** the chip labels `you`, `friends`, or the Table name. Only a non-you scope is terracotta-active. A Table chip shows up to two cached member faces; the sheet lists the viewer, friends, and the viewer's Tables with cached counts only, checks the active row, and closes on selection. `network_map_pins` arms only for `friends`; `wishlist?action=list_table` arms only for a Table scope; `useTableMembers` mounts only for the active Table chip and unmounts while its picker is open.
- **Locked Table scope:** `/places-scope?scope=table&tableId=…` renders the same body against a private state-store instance, with a back chevron, no picker, no import pill, and no bottom nav. The Table header's map control pushes this route, so back returns to its pusher. The locked screen may change its view, layer, and camera without mutating the tab snapshot. Search remains world-wide rather than Table-scoped.
- **Browse Lists shelf:** switching to Lists leaves the current detent and last committed Places map unchanged. One sheet-connected native list renders Your lists, `new list`, then Saved lists with author metadata; taps push one level to `/list/[id]`, so back returns with Lists still active. The shelf distinguishes loading, my-lists cold failure, settled empty (`no lists yet` plus creation), rows, and warm/partial retry states. Saved lists are requested only while the zero-query shelf is visible.
- **Friends scope:** selecting friends alone arms caller-scoped `user-profile?action=network_map_pins`; `you` and Table scopes make zero network-pin calls. Avatar-face markers carry the followee entry payload. A carried primary-author rating renders as `<value> · <first name>` in the row and selected caption; without a rating, the right column keeps the author's first name alone. The relationship line remains `<friend> · N friends been`. Empty reads `nothing from friends yet`; cold and warm network failures use full and inline retry respectively. Row and caption taps always open the restaurant page.
- **Clip doorway:** the unchanged 48×48 download pill rests with no attention, shows an indeterminate terracotta ring while any local manifest is `reading`/`saving`, or shows the exact first-page needs-look item count when nothing is clipping; clipping wins and `review`/`kickoff`/`failed` manifests stay visually quiet. Search mode removes the whole pill without shifting the search or chip rows. Every pill state opens the warm-paper clip tray: a real `SnapSheet` starts at 64% height, drags up to the safe-area full detent, hands off to scrolling, and drags down to dismiss. `done` is the accessible alternative to the drag; URL focus expands above the keyboard. `ImportLinkSheet` remains nested in the same native Modal. The short link placeholder and two-line row titles avoid clipped instructions/names. The badge reads `N to check`. `CLIP A PLACE` launches the existing URL/video/screenshot import paths through a nested `ImportLinkSheet`, and `LANDED` renders live local manifests followed by newest saved batches. Screenshot destination failures render one lowercase inline line above the primary action and clear on retry, dismissal, or a fresh resolve; the Table-share destination does not inherit that line. Needs-look rows group exhausted items by `job_id`; at most five rows render, then `older ·` opens `/import-progress`. No-history is intended-empty with the single line “what you clip lands here.” Batch/detail pushes wait for tray dismissal, so `/imports/[jobId]` back returns one level to Places with the tray closed. The mounted first exhausted page polls at 60s; recent imports never poll and update from the existing exact invalidations.
- **Focused search / sections:** focusing the shared field or arriving with `q` unmounts both `WishlistMapView` and the snap sheet; search is a full paper page. Focus replaces the search glyph with a back chevron, keeps locality visible, hides browse chips/import/filters, and places `places · lists · people` directly above the pane. Empty Places search renders only data-bearing sections in order: RECENT (max 8, stored query strings, no thumbnails), NEAR YOU (nearest 6 from pinned ∪ been; a 44pt stored-photo thumbnail only when the carried row has a recognized source, with attributed Places photos credited and unknown/uncredited sources failed closed), YOUR LISTS (max 4, tinted emoji/icon plates), then PEOPLE YOU FOLLOW (56pt avatars and first names, followees only). The people rail is absent under `FRIEND_TEST.hidePeopleSearch` or at zero follows. NEAR YOU is absent without auto-locality coordinates and in explicit-city mode. One character shows the threshold line; clearing returns to sections. Back clears the query and remounts the browse map at its saved camera.
- **Searching / results:** two or more characters replace focused sections and enable both `places-search` and `restaurant-history?action=search` only while the `places` segment is active. Rows show only carried Napkin numbers: the viewer's value, a friends-tier value with `· friends`, or a network primary-author value with `· <first name>`; an external tier or no Napkin value leaves the cell empty. Lists and People retain their existing search panes on the paper surface. A cold failure from either search source renders broken-empty; a failed refresh keeps merged cached rows with inline retry.
- **Selected pin / ghost pin:** marker selection remains controlled by per-user `placesScreenState`; `PlacesSelectedCard` opens the same route as its row. Its compact horizontal card uses an 80×80 photo and leads with an attributed venue image, then authorized meal/clip media. It shows a single-line name, cuisine/known price/Napkin rating together, and address/city with distance or friend context. Photo credit sits beside the image. Hours remain on the restaurant page. A selected-only `usePeekCard` reads `restaurant-history?action=peek_card` with the viewer and correct saved/been/network/Table context; it does not buy a Places lookup or persist a ghost. Carried facts render during loading/failure. Missing or failed photos settle to a symbol-only no-photo plate with a spoken no-photo label; uncredited/unknown photo sources fail closed, and surviving Places authors are credited. The card resets media failure state and scroll position on viewer/scope/place changes and follows the sheet with the existing UI-thread transform. Its scroll viewport uses at most 40% of the measured free space between map controls and sheet, preserving most of the map even at large text sizes. Long credits and scaled content remain reachable by scrolling. Marker outer bounds and glyph metrics stay fixed across selection; enlargement uses a transform inside the annotation, keeping its tail anchored and its native identity stable. Opening a row or pin carrying a full sanitized Place never buys a duplicate Details call. Text-search ghosts are explicitly marked deferred, so the restaurant page may buy one required Details enrichment when their payload is still thin (`googleRating == null` is thin); a full enriched payload keeps the production lookup gate disabled.
- **Map / list lifecycle:** the Places sheet still exposes about 250pt at browse peek, but the bottom-left `list` pill now switches to a full-height paper ledger and unmounts both the map and sheet. In map mode, the `list` pill, locate FAB, and selected-place caption follow the sheet's live drag/spring position on the UI thread and remain below the sheet in paint order; native map padding changes only when the detent settles. Rows group by city; explicit locality leads, otherwise the nearest-row city leads when coordinates exist, otherwise `home_city` leads; remaining groups sort by count, `ELSEWHERE` trails, and rows sort by distance or name. The last group alone carries the pagination `+` until exhausted. A fixed frosted `map` pill above navigation remounts the map. Search also forces map view and hides the toggle. `placesScreenState.region` receives `WishlistMapView.onRegionChangeComplete` and returns through `initialRegion`; restoration skips only the mount-time collection reframe, while later scope changes retain normal framing.
- **Pagination / repair / share:** the `you` scope's all and pinned browse ledgers page the 40-row wishlist cursor on end reached and show a `+` on the last group until exhaustion; been, friends scope, Table scope, Lists, People, and every query never request the next wishlist page. Coordinate-less loaded pins surface the existing repair murmur/sheet only under `you` all or pinned. The pinned-only share icon is map-mode only and opens the unscoped wishlist handoff with the unfiltered loaded pinned total even when facets narrow the visible ledger.
- **Recent searches:** successful per-user search queries appear in the focused-empty RECENT section. Selecting one searches again; clear removes the scoped history. This is the reader paired with `searchCache.addRecent`.
- **Lists search / shelf:** `mode=lists` selects Lists, focuses the shared field, and explicitly clears stale immediate, debounced, and stored query state when `q` is absent. Zero characters show the same Your/Saved shelf as browse, exactly one character shows `type one more letter`, and two or more reuse `ListsSearchPane` public results.
- **People search:** reuses `PeopleSearchPane` on the full paper takeover. Its own empty query still suggests the viewer's following list plus unfollowed `co_diners`; the PEOPLE YOU FOLLOW rail belongs only to the empty Places pane and never adds strangers. An arrival without `q` clears stale query state. Both restaurant queries are disabled, including while typing. Leaving search restores the prior browse detent and saved camera.
- **People-hidden fallback:** with `FRIEND_TEST.hidePeopleSearch`, `mode=people` still reveals the two-segment header but selects Places in focused FULL mode and permits restaurant search.
- **Locality / distance:** auto mode uses granted coordinates for bias, the native dot, locate, distance, and nearby order. A selected city sends only `city`; real device coordinates remain only the native dot/locate target. Explicit-city and denied/no-location rows/captions omit distance, preserve source/server order, and frame the result collection without geocoding.
- **Framing stability:** result-collection framing depends on a stable selection dispatcher whose latest parent callback lives in a ref. An inline `onSelectedChange` identity change cannot cancel the pending 260ms fit. A supplied restored region suppresses the first collection fit/animation only; a later collection identity still reframes.
- **Per-user isolation:** query, selection, scroll, snap, view mode, saved camera region, active segment (including Lists), layer filter, who scope, search/People detent restoration, locality, caches, and recent-search storage reset at auth identity change. Each locked scope uses a separate factory-created snapshot, so its round trip cannot contaminate the tab instance. The old device-global recents key is deleted, never migrated.

### Setup and feature discovery (13 September 2026)

- Setup uses `components/onboarding/SetupFrame.tsx` and `OnboardingProgress.tsx`: three steps (optional name, mandatory moderated photo, optional city), with conditional people suggestions labelled optional. The name step is skipped entirely when Apple/Google already supplied one, and when it does render Continue is enabled on an empty field with a Maybe later skip beside it (App Store Guideline 4, 2026-09-14). It keeps the standard `Make yourself at home / 1 of 3` header and must NOT pass SetupFrame's `optional` prop: that prop is `OnboardingProgress`'s follows-step copy switch, not a general flag, and passing it relabels the header `People you know / Optional`. Back controls, upright headings/fields, scrollable paper cards and a keyboard-safe footer are shared. Existing moderation and server completion gates remain authoritative.
- `HomeCityPicker` opens a full-screen, compact local search above the keyboard with up to five visible suggestions. Picking a city or Done commits only the local field; Cancel preserves the prior city. Free-text cities and clearing remain supported. City search does not finish onboarding, fetch remote suggestions or affect the Settings city field. Standard, no-match, selected, cancelled and large-text scroll states require native verification.
- `useFinishOnboarding` preserves pending/error/retry and city override semantics. Its name falls back draft -> provider metadata -> `null`, and sends `null` (never `''`, never a hardcoded `'New User'`), so `fn_complete_onboarding` leaves `profiles.display_name` untouched. **Provider-confirmed identity only — never derive a name from the login email here.** This same action stamps `account_privacy = 'public'`, so a derived name would publish an identity the user had just declined to give. The consequence is that a user who skips the name keeps the trigger's literal `'New User'`, which renders raw as their profile name and as an `NU` avatar monogram until they set one in Settings -> Name; normalizing that at the render sites is open follow-up work, not something to paper over here. Only confirmed completion opens `/welcome?intro=1`; preview opens `preview=1`. No launch-wide redirect is added and existing users are not enrolled automatically.
- `/welcome` teaches journal, pins, sharing and Tables in four skippable chapters on iOS; Android retains journal, pins and Tables, with no sharing chapter, demo, or sharing deep-link destination. The sharing chapter features TikTok/Reels artwork and a dismissible interactive `ShareWalkthrough`: source Share → TikTok More / Instagram Share to → iPhone More → Apps / Napkin → add for review → return to the source → open Napkin’s clip tray → select/review restaurants → saved map. The source scene is a bundled still with source-specific chrome; dark source/iOS drawers slide over the same scene with aligned native Pressable targets, then transition to Napkin’s paper review and illustrative map. All steps and selections are offline examples; the demo cannot import, queue, share or save anything. Its completion advances to Tables in the first-run intro and returns to sharing in replay. The same demo is replayable from `/settings/import-tutorial`. The replay directory also covers lists and following. Rounds, Top Fours and Atlas are not introduced because they are hidden. Tables are private groups; public-profile pins and eligible reviews can remain visible outside them.
- `DiscoveryTip` is an inline, dismissible card in the personal pinned Places list, Feed header, active Table header, own Diary and Lists. `lib/discoveryGuide.ts` + `useDiscoveryGuide` hydrate versioned user-scoped AsyncStorage; enablement and dismissals merge in-flight reads and serialize writes. No DB/query shape changes. Existing users without guide enrollment, signed-out viewers and not-ready/disabled/dismissed states render nothing. Enrolled users retain their remaining tips on later launches. Persistence is best-effort and tips never gate app access.
- `TableIntroduction` replaces zero-Table dead-end copy with a labelled example, three benefits and existing creation/guide navigation. A failed Table query still renders retry rather than this intended-empty state.
- Local enable/dismiss controls are SAFE (device preference only); guide/back/skip/feature links navigate only. Start a Table opens the existing composer; its final submit remains LIVE-WRITE and must never be exercised against production during verification. Photo and follow controls retain their existing LIVE-WRITE restrictions.

## 4. Write paths

`SAFE` means self-scoped and reversible or device-local. `NEVER` means irreversible, shared, sends/notifies another person, changes moderation/visibility, or deletes. **Live verification remains read-only for both labels.** Read-only navigation controls are omitted unless they request an OS permission or launch outbound contact.

| Surface / mutating control | Call / storage | Refusal conditions enforced by UI/server | Live safety |
|---|---|---|---|
| Auth: sign in / OAuth | Supabase Auth via `providers/AuthProvider.tsx`, `app/auth.tsx` | valid credentials/provider result; timeout/error stays on auth | SAFE (session-scoped) |
| Auth: sign up, password-reset email | Supabase Auth | valid email/password; rate/provider errors | NEVER — creates identity or sends email |
| Auth: sign out | `AuthProvider.signOut`; clears session/query state | authenticated session | SAFE |
| Entry: create log/note, upload photos | `useCreateEntry` → EF `entry`; RPC `fn_create_entry_with_tables`; storage/RPC photo append | auth, restaurant/note contract, rating when required, uploads settled, selected Table memberships; missing visibility derives `'table'` with any Table id and `'friends'` otherwise | NEVER — creates visible/shared content |
| Entry: edit, merge restaurant, attach to Supper | EF `entry` update/merge/attach actions | author; target/membership/restaurant compatibility; valid Supper | NEVER — changes shared content |
| Entry: tag companions | `CompanionPickerSheet` → EF `entry` create/`update-companions`; EF `user-profile` search/recent actions | author; not a mutual follow / blocked → dropped server-side | NEVER — grants entry access and may notify |
| Entry: delete | EF `entry` or `account` path | author/authorized owner; confirmation | NEVER — deletion |
| Entry: like/unlike | `usePostInteractions` → EF `post-interactions` | auth and permission to view eligible entry | NEVER — social signal/notification |
| Entry: add/edit/delete comment, comment-like | EF `post-interactions` | auth; table membership for Table content; public eligibility and reply-permission gates | NEVER — sends or deletes social content |
| Entry: report/block author | EF `account` | auth; valid non-self target/content | NEVER — moderation/relationship impact |
| Wishlist: pin/unpin own spot | EF `wishlist`; pin sheets on restaurant/search/import | auth, canonical restaurant; dedupe | SAFE — self-scoped/reversible, but publicly visible when account is public |
| Wishlist: fix/repoint unmappable spot | EF `places-search`/`wishlist` correction | auth, owned/import row, valid replacement | SAFE — self-scoped/reversible data repair |
| Wishlist: local filters, source/list choice, clear local import history | component/device state | source available; no server membership change | SAFE |
| Places locality sheet / Map: request location | locality sheet or Map → `useNearbyLocation` → OS permission | user-initiated; platform permission state | SAFE only on disposable simulator/device |
| Places clip tray: paste URL / choose video or screenshot | nested `ImportLinkSheet` → existing `resolve-url`, local import queue, `wishlist`, list/Table destination paths | authenticated owner, valid URL or selected media, valid candidates/destinations; Table membership for a shared destination | NEVER — starts an import and can create public saves, list entries, or Table shares |
| Import: enqueue URL/video, approve/save, retry, exclude/repoint | local manifest + `resolve-url`, `wishlist`, `lists` | auth/owner manifest, valid URL/candidates/destination; Table membership for shared destination | NEVER — creates saves/list/Table shares and may notify |
| Sharing practice: source choice, highlighted share targets, restaurant selection, back, close, finish | component state only in `ShareWalkthrough` and `share-demo/*` | a target only advances its own current step; repeated/stale targets cannot skip steps; Save requires at least one selected example; close/reopen resets the rehearsal | SAFE — bundled example assets; no native Share, URL opening, queue, account or network calls |
| Import: share-extension handoff | App Group/import protocol in `app/import.tsx` | signed-in owner or pending-auth handoff; valid manifest | NEVER — begins a write workflow |
| Profile: edit name/bio/city/photo/username/top fours/takes | `user-profile`, `top-fours`, `moderate-image` | auth/owner; format, availability, image moderation, city/entry constraints | SAFE — self-scoped/reversible, but changes public profile |
| Profile: switch privacy/reply permission | EF `user-profile` | auth/owner; making public requires valid username (`settings/privacy/make-public.tsx`) | NEVER — changes audience/safety policy |
| Profile: follow/unfollow | EF `user-profile?action=follow/unfollow` | auth; not self; block constraints | NEVER — relationship and possible notification |
| Profile: block/unblock | EF `account` | auth; not self; existing block as appropriate | NEVER — relationship/visibility impact |
| Lists: create/update personal list | EF `lists` | auth/owner; valid title/privacy | SAFE only for private personal list; otherwise NEVER |
| Lists: create/update Table/public list | EF `lists` | auth; Table membership/creator permission; valid title | NEVER — shared/public content |
| Lists: add/remove/reorder/note an entry | EF `lists`; RPC `fn_add_list_entries_canonical` | owner or permitted Table member; canonical restaurant; list limits | SAFE only for private personal list; otherwise NEVER |
| Lists: save/unsave another list | EF `lists` | auth, visible list, not disallowed by privacy/block rules | SAFE — self-scoped/reversible signal |
| Lists: delete list | EF `lists` | owner/authorized Table list creator; confirmation | NEVER — deletion |
| Handoff: create/share/revoke/accept | EF `handoff`, `resolve-url?action=save_spots` | auth; live nonexpired token; owner for revoke; destination authorization | NEVER — outbound/shared write |
| Tables: create Table | EF `table-management` | auth; non-empty valid name | NEVER — creates private group |
| Tables: invite/add/approve/decline member | EF `table-management` | auth; owner/recipient gates; mutual-follow/existing/pending checks where applicable | NEVER — sends/changes membership |
| Tables: rename/change cover or top fours | EF `table-management`/`top-fours` | owner/member role and valid payload | NEVER — shared group state |
| Tables: leave Table | EF `table-management` | authenticated member; owner cannot use leave path | NEVER — membership loss |
| Tables: delete Table | EF `table-management` | owner and confirmation | NEVER — deletion |
| Tables: mark timeline seen / dismiss local nudge | EF seen marker or component state (`app/(tabs)/tables.tsx`) | member/current viewer | SAFE — self-scoped read marker/local state |
| Table share/float: create, share, dismiss/remove | table activity/share endpoints | membership, visibility, author/recipient gates | NEVER — shared timeline state |
| Supper: create/set Table/attach entry/add take | EF `entry` Supper actions | auth; Table membership; matching restaurant/Table; roster/seat and duplicate-entry checks | NEVER — shared group meal |
| Supper: delete | EF `entry?action=delete-supper` | host/authorized owner and state gate | NEVER — deletion |
| Gather: create | EF `gatherings?action=create` via `GatherSheet`, opened from the restaurant Details `gather the table` row | auth, selected Table and restaurant, membership, no duplicate active Gather | NEVER — sends to Table |
| Gather: RSVP/counter | EF `gatherings` | invited member; proposed/live state; valid option/date | NEVER — sends shared response |
| Gather: reschedule/cancel/clear | EF `gatherings` | host; lifecycle-specific gates; deletion/lock checks | NEVER — shared state/deletion |
| Gather: rescue into Supper | EF `gatherings?action=rescue` | host, expired and unrescued Gather, valid Table/crew/restaurant | NEVER — creates shared Supper |
| Legacy round: start/join/rate/ready/reveal | EF `table-night` and RPCs | auth, Table membership, legacy lifecycle/rating gates | NEVER — shared legacy state; do not use as product verification |
| Notifications: mark one/all read | EF `notifications` | authenticated recipient | SAFE — self-scoped read state |
| Notifications: accept/decline Table invitation | EF `table-management` | authenticated intended recipient; invitation pending | NEVER — membership change |
| Notifications: enable notifications | OS prompt from `NotifPermissionSheet` | soft-prompt cadence and current OS permission | SAFE only on disposable simulator/device |
| Settings: delete account | EF `account?action=delete-account` | auth and destructive confirmation | NEVER — irreversible deletion |
| Admin: create/edit/delete critic review | EF `critics-admin` | `admin_users` gate; valid critic/restaurant/review payload | NEVER — moderation/editorial production write |
| Restaurant: directions/website | `RestaurantDetails` URL/address rows and `RestaurantLocationPreview` | nonempty directions URL; website field; valid coordinates for map preview | SAFE read-only outbound navigation |
| Restaurant: call | `RestaurantDetails` `tel:` handler | phone number available | NEVER — can contact a real business |
| Search/people/list: SMS or system-share invite | Linking/share APIs in search/profile/list components | target/contact/share payload available | NEVER — sends to others |

Composer visibility (fixed 2026-08-26, TICKET-217 P0-1/P1-4): `lib/composer.ts::buildEntryPayload` emits `visibility='friends'` with zero selected Tables and `'table'` otherwise — matching `components/logging/FastLogForm.tsx` and the 2026-07-22 founder order (never `'private'` as a silent default). The atomic SQL writer `fn_create_entry_with_tables` independently applies the same Table/friends fallback when visibility is absent. `lib/composer.test.ts` asserts the client contract. A public account sees "also appears on your public profile" at save time in both `app/log-meal.tsx` and `app/create-entry.tsx`.

Successful entry creation, editing, deletion, Supper-take creation, and legacy merge creation invalidate the exact `restaurants.page(restaurantId)` prefix plus that viewer's restaurant history and the restaurant reviews key. This refreshes restaurant counts and memories without blanket-invalidating every restaurant page.

## 5. Known illusions and incidents

| Looks broken / incident | What the code says and how to verify | Pointer |
|---|---|---|
| An onboarding step's header says "People you know" on the wrong screen | `SetupFrame`'s `optional` prop is not a generic "this step is skippable" flag. `OnboardingProgress` uses it as the follows-step COPY switch, replacing the header with "People you know" and the counter with "Optional". Only `follows.tsx` may pass it; `city.tsx` is skippable too and does not. A skippable step signals itself with a `Maybe later` control, not with this prop. | `napkin-app/components/onboarding/OnboardingProgress.tsx`; `napkin-app/components/onboarding/SetupFrame.tsx`; `napkin-app/app/onboarding/index.tsx` |
| Onboarding starts at the photo step and never asks for a name | Intended. Apple hands the user's full name to the app ONCE, in the native credential on the first authorization for that Apple ID, out-of-band (it is not a claim in the identity token, so no server-side trigger can ever see it). `auth.tsx` now captures it, persists it to `auth.users.raw_user_meta_data.full_name` via `updateUser` and stashes it locally, and `useProvidedDisplayName` then skips the name step. Asking again was the Guideline 4 rejection of 2026-09-14. Verify: sign in with a fresh Apple ID and confirm the first screen is the photo step with the name already on the avatar card. | `napkin-app/lib/oauth.ts`; `napkin-app/lib/pendingIdentity.ts`; `napkin-app/lib/onboardingName.ts`; `napkin-app/hooks/onboarding/useProvidedDisplayName.ts`; `napkin-app/app/onboarding/index.tsx` |
| The name step appears anyway after Sign in with Apple | Also intended, and it must never block. Apple returns `fullName: null` on every authorization after the first, so an App Review reviewer whose Apple ID already authorized Napkin cannot be given a name by the framework. The step then renders labelled `Optional`, with Continue enabled on an empty field and a `Maybe later` skip — App Review Guideline 5.1.1(x) allows asking only when the request is "optional for the user" and features are "not conditional on providing the information". A disabled Continue here is a regression, not a polish item. | `napkin-app/app/onboarding/index.tsx`; `napkin-app/lib/onboardingName.ts`; `napkin-app/hooks/onboarding/useFinishOnboarding.ts` |
| The Apple button's typeface does not match the Google button beside it | Intended and non-negotiable. `AppleSignInButton` renders Apple's own `AppleAuthenticationButton`; Apple permits changing only its `cornerRadius`, so its SF Pro label cannot be restyled to Manrope. The previous custom Pressable drew an Ionicons `logo-apple` glyph (a redrawn logo, forbidden) and used `Type.label`, whose `textTransform: 'uppercase'` rendered "CONTINUE WITH APPLE" — not one of Apple's three permitted strings. Do not "harmonise" this button. | `napkin-app/components/auth/AppleSignInButton.tsx`; `napkin-app/app/auth.tsx` |
| Settings → delete account shows `deleting…`, signs out, but the account survives (2026-09-07 to 09-09; the App Review recording hit it) | GoTrue deletes `auth.users` as `supabase_auth_admin`. Cascades run their DELETE as the table owner but queue AFTER ROW triggers with `fire_triggers=false`, so those fire as `supabase_auth_admin`, which holds no grant on `public.*`. Any SECURITY INVOKER trigger body that touches another table then fails (first 42P01 on an unqualified name, then 42501), the auth delete aborts, and `account_deletions` sticks at `purging`. The four table-touching trigger functions that fire on the cascade (`cascade_delete_post_interactions_extended`, `sync_post_counts_and_top_emojis`, `sync_comment_like_count`, `touch_list_updated_at`) plus their two dead legacy twins are SECURITY DEFINER now. `fn_entries_mirror_table_id_to_join` must STAY SECURITY INVOKER: its trigger is `UPDATE OF table_id` (never fired by the cascade) and its `entry_tables` RLS violation is the Table-membership gate. Verify: `supabase/tests/account_deletion_cascade.spec.sql` (deletes a seeded user as `supabase_auth_admin`); in prod, `logs` source `auth_logs` action `user_deleted` must carry no `error`. A stranded row needs the `account_cleanup` moderation job, which only runs when that workflow is activated. | `supabase/migrations/20260907190000_pin_search_path_on_trigger_functions.sql`; `supabase/migrations/20260909120000_security_definer_on_auth_cascade_trigger_functions.sql`; `supabase/functions/account/deletionSaga.ts` |
| The app jumped back to Places and flashed "Checking your account…" mid-use (until 2026-09-23) | Every Supabase auth event, including the hourly `TOKEN_REFRESHED` and the refresh on return after an hour away, re-ran the onboarding-gate read, which first blanked `onboardedAt`. A blank gate unmounts every signed-in screen, so navigation restarted at `/` (Places) and open drafts were lost. `AuthProvider.syncOnboardingGate` now re-reads only for a different identity or a gate that never got an answer. Verify with a forced `supabase.auth.refreshSession()` in a local build while on a non-Places tab: the screen must not change. | `napkin-app/providers/AuthProvider.tsx`; `napkin-app/providers/AuthProvider.test.tsx` |
| Bottom ~116 pt of the **first mounted screen** painted but ignored taps | A stale `react-native-screens` content-wrapper frame could be shorter than its screen. `patch-package` now synchronizes the wrapper size except the `formSheet` case. Test bottom controls on the first screen after cold launch. | `napkin-app/patches/react-native-screens+4.16.0.patch`; `/Users/jacky/Napkin/.kanban/review/TICKET-212-first-screen-dead-touch-band.md`; PR #329 |
| iPad auth looked frozen/clipped | Auth is scrollable/keyboard-safe and async sign-in legs have timeout/error recovery. Verify compact and iPad widths without issuing live auth writes. | `napkin-app/app/auth.tsx`; `/Users/jacky/Napkin/.kanban/done/TICKET-211-ipad-review-freeze-hardening.md`; PR #328 |
| `PGRST201` after multi-Table entries | Multiple `entries → tables` relationships made implicit PostgREST embeds ambiguous. Server code names the intended FK or queries/batch-hydrates explicitly. The same rule prohibits pretending `entries` embeds `profiles`. | `supabase/functions/restaurant-history/index.ts`; `/Users/jacky/Napkin/.kanban/done/TICKET-043-multi-table-per-entry.md` |
| Retired wishlist or Table-map link opens no screen | The two legacy route modules were intentionally deleted when Places became the single map. All old wishlist destinations must target `/(tabs)/places?view=list&layer=pinned`; the Table tab's single map control is the `on the map` row on its Activity pane, which pushes `/places-scope?scope=table&tableId=…`. | TICKET-237; `napkin-app/lib/handoffNavigation.ts`; `napkin-app/app/(tabs)/tables.tsx`; `napkin-app/app/places-scope.tsx` |
| Map crashes only under New Architecture | The maps patch guards stale/out-of-range child insertion and nil subviews in Apple/Google map managers. `postinstall` must have run before reproducing. | `napkin-app/patches/react-native-maps+1.20.1.patch`; `napkin-app/components/wishlist/WishlistMapView.tsx` |
| Settings field autofocus flickers or steals focus | New Architecture transitions can outlive `InteractionManager`; `useFocusAfterTransition` waits for native `transitionEnd` with a fallback before focusing. | `napkin-app/components/settings/useFocusAfterTransition.ts`; `/Users/jacky/Napkin/.kanban/done/TICKET-158-settings-editors-heirloom.md` |
| A chooser opened from a sheet appears behind it or fails to present | Under Fabric/iOS, a stacked `Modal` must be nested inside the currently open `Modal` so it presents from the right view controller. | `napkin-app/components/profile/ProfileTopFourSheet.tsx`; `napkin-app/components/wishlist/UnmappedSpotsSheet.tsx`; `napkin-app/components/gatherings/GatherSheet.tsx`; `/Users/jacky/Napkin/.kanban/done/TICKET-186-list-detail-sheet-over-map.md` |
| Previous user's cached rows flash after account switch | Viewer-specific keys must contain the viewer ID, and owner-bound local import state is fenced/reset when auth identity changes. Explicit sign-out also clears query state; an identity swap without reset is a bug. | `napkin-app/lib/queryKeys.ts`; `napkin-app/providers/AuthProvider.tsx`; `napkin-app/lib/importOwnerGuard.ts`; `/Users/jacky/Napkin/.kanban/done/TICKET-199-table-map-parity.md` |
| Restaurant detail shows a typographic masthead instead of a photo | The resolver found no valid attributed Places hero. User review photos and clipping thumbnails do not become the restaurant’s opening image. This is an intended fallback, not a loading state. | `napkin-app/lib/restaurantPhoto.ts`; `napkin-app/app/restaurant/[id].tsx`; `napkin-app/components/restaurants/RestaurantPageV3.tsx` |
| Restaurant ratings do not combine | Overview uses the server Napkin aggregate and separately counts written reviews. Google remains an external Details fact and never enters that aggregate. Table notes and public review previews remain separate authorized rings. | `napkin-app/components/restaurants/RestaurantOverview.tsx`; `napkin-app/lib/restaurantPageV3.ts`; `napkin-app/components/restaurants/RestaurantPageV3.tsx` |
| Restaurant Napkin count or photo rails differ by viewer | This is privacy filtering, not missing data. The service-role `restaurant-history` endpoint sends separate aggregate and photo batches through `fn_visible_entry_ids`. The aggregate filters on privacy only (blocks, private accounts, and private visibility), never note length; photos additionally require canonical review-content parity. Authorized Table/companion/Supper scopes remain. | `supabase/functions/restaurant-history/index.ts`; `supabase/migrations/20260826155643_ticket_217_restaurant_privacy_gates.sql` |
| Only a few notes appear inline per restaurant ring | The reviews preview caps at three cards and the chosen-Table section at two. Their independent “all” doorways lead to public reviews and the exact Table Activity pane respectively. | `napkin-app/components/restaurants/RestaurantPageV3.tsx`; `napkin-app/lib/restaurantPageV3.ts` |
| Journal includes Table-shared entries despite `useMySoloEntries` name | This is current scope by design: all entries authored by the user, no Table filter. | `napkin-app/hooks/entries/useMySoloEntries.ts`; `supabase/migrations/20260616000100_my_journal_all_entries.sql` |
| Feed has no emoji reaction picker | Heart-only like/unlike is deliberate; legacy emoji-shaped backend fields remain compatibility data. | `napkin-app/components/feed/FeedActionRow.tsx`; `supabase/functions/post-interactions/index.ts` |
| Round/table-night cards or routes exist | They are legacy/dead product code. New group meals are Suppers grouped by `supper_id`. | `napkin-app/app/table-night.tsx`; `napkin-app/app/table-night-detail.tsx`; `napkin-app/hooks/suppers/index.ts`; `supabase/functions/entry/index.ts` |
| Private profile looks like a partial/broken profile | `private_stub` is an explicit server variant; a blocked view is a separate `blocked_by_viewer` variant. | `napkin-app/components/profile/ProfileScreenBody.tsx`; `supabase/functions/user-profile/index.ts` |
| Saved pin appears on somebody's profile without a review | Saves are standalone public-by-default signals gated only by account privacy, read by `fn_restaurant_saves_visible`. | `supabase/functions/restaurant-history/index.ts`; `supabase/migrations/20260710160000_fn_restaurant_saves_visible.sql` |
| A Google Maps share imported nothing ("couldn't find spots"), while a Maps link pasted from the web worked (until TICKET-248) | Links made by the Google Maps app redirect three times before a slow 220 KB page. `expandMapsShare` followed the whole chain under one 2.5 s abort and read the place only from the final URL, so every app share timed out empty. It now reads the first hop. Verify: `resolve-url/mapsList.test.ts` app/web/list/consent cases; live, a Deno probe of an app-made `maps.app.goo.gl` link returns the `?q=` name in under a second. | `supabase/functions/resolve-url/mapsList.ts`; `supabase/functions/resolve-url/index.ts` |
| A shared TikTok/Instagram sat on "processing in background" for hours, then failed (builds 262+, until TICKET-248) | Share uploads created server jobs, and the app refused to process a share locally while its server job was pending. The worker ran once at enqueue; when that attempt failed, the next came from the scheduled rescue, which GitHub runs every two to five hours, not every five minutes. After three failed attempts the job failed. The server now marks every enqueued job `needs_device`, so the app processes on the phone at once. | `supabase/functions/background-imports/index.ts`; `.github/workflows/restaurant-completeness-cron.yml` |
| Import progress remains on “reading” | On-device OCR/perception and background list draining can legitimately outlive a navigation transition; inspect manifest phase and job heartbeat before calling it stuck. | `napkin-app/hooks/wishlist/useProcessImportQueue.ts`; `napkin-app/lib/largeImportJob.ts` |

### Doctrine/code contradictions visible at time of authoring

These are not silently resolved here; agents should preserve the code truth and escalate the product mismatch.

1. **Bottom-nav `+`: RESOLVED by TICKET-228.** The founder-approved bar is FEED · TABLE · PLACES · PROFILE with 24px outline Ionicons and uppercase labels. There is no floating `+`; restaurant detail remains the write doorway.
2. **Solo visibility default:** RESOLVED 2026-08-26 (TICKET-217 P0-1). All composer paths (`lib/composer.ts`, `components/logging/FastLogForm.tsx`) now emit `'friends'` for solo / `'table'` for table-shared, and the server paths in `supabase/functions/entry/index.ts` (`merge_with`, `attach-take`) fall back to `'table'`, never `'private'`. If any path is ever seen emitting `'private'` as a default again, that is a regression against the 2026-07-22 founder order — flag it, do not preserve it.
3. **Map count bubbles:** doctrine says the Map has no cluster/count bubbles. `components/wishlist/WishlistMapView.tsx` correctly avoids zoom-out clustering and renders individual pins, but deliberately renders an upright amber numeric face for Table overlaps where `overlap.count >= 2`. Cluster bubbles are absent; semantic overlap count bubbles remain.

## 6. How to reach a state

All recipes are read-only unless they explicitly say **fixture/test only**. Prefer an existing seeded review identity from section 1; never manufacture these states in live data.

### First run / onboarding

1. In an isolated test or disposable local Supabase fixture, authenticate a user whose `profiles.onboarded_at` is null (`providers/AuthProvider.tsx`, `app/_layout.tsx::RootLayoutNav`).
2. Cold-launch. The gate routes to `/onboarding`; advance through photo and city/follows using mocked Edge Function results.
3. Capture the name step in both of its forms — skipped entirely when a provider supplied a name (stash a `pendingIdentity` entry or a `user_metadata.full_name` for the fixture user and confirm the stack opens on photo), and rendered `Optional` with Continue enabled on an empty field plus `Maybe later` when it did not — then the mandatory photo (empty, uploading, approved, rejected/unavailable), city (local suggestions, no matches, keyboard), optional follows (loading, candidates, failure rollback), and completion pending/error/retry states. City suggestions are local; only co-diner/completion calls have network failures. All remote writes use isolated fixtures.
4. Successful real completion alone opens `/welcome?intro=1`; onboarded preview opens `/welcome?preview=1` without enabling tips or completing the profile again. Capture journal → places → sharing → Table chapters, back/skip, terminal Places and optional Tables destinations. New accounts must not be able to reach the welcome before profile completion, and import/handoff/join-table resume priority remains unchanged.
5. Settings → A guide to Napkin opens the five-chapter directory. Capture every chapter, invalid-topic fallback, close/back hierarchy, privacy doorway and feature destinations. All preview cards are clearly labelled Example and use local bundled assets.
6. Enable discovery only for a local fixture viewer: capture Places personal pinned list, Feed, active Table, own Diary and Lists tips; dismiss each, remount and confirm it stays absent. Verify a different/signed-out/existing-not-enabled account sees no tip. Tips never overlay the map or show on another person's Diary. Storage failures do not block navigation or lose session dismissals.
7. Capture zero-Table introduction and its Start a Table/How Tables work navigation without submitting creation; keep loading and cold error/retry separate from intended-empty. Verify comfortable scrolling on a small iPhone, keyboard-safe setup footer, large text and dark mode.

### Launch screen states

1. Cold launch: `xcrun simctl terminate <UDID> com.majilaii.dining-journal-app`, then start `xcrun simctl io <UDID> recordVideo` and launch. The sequence lasts about 1.5 s, faster than `simctl io screenshot` can sample; step through the recording with `ffmpeg` instead.
2. Slow launch: throttle the Mac with Network Link Conditioner (Very Bad Network) and cold-launch; `Setting the table…` appears after 3.5 s.
3. Offline cold launch: disconnect the Mac from the network, cold-launch, capture `No connection`, then reconnect and confirm the cover carries on to the app.
4. Account unreachable and the sign-in wait with a failing profile read: **test only** (`components/launch/__tests__/LaunchProvider.test.tsx`, `providers/AuthProvider.test.tsx`); it needs three failed profile reads while reachability passes.
5. Native splash handoff: needs a native build (`npx expo run:ios`); compare the last native-splash frame with the first launch-screen frame of a recording.

### Empty journal

1. **Fixture/test only:** return `{items: [], next_cursor: null}` from RPC `fn_my_solo_entries` for an authenticated user (`hooks/entries/useMySoloEntries.ts`).
2. Open `/journal`; capture the empty journal CTA.
3. In a separate case reject the RPC and capture the error/retry state. This pair proves intended-empty versus broken-empty.

### Private account viewing

1. Use two fixture identities. Set the subject's `profiles.account_privacy` to private and leave the viewer unauthorized by the server relationship rules (`supabase/functions/user-profile/index.ts`).
2. Open `/u/[username]`; the response variant must be `private_stub`, not an empty normal profile.
3. Separately seed `blocked_users` with viewer as blocker to reach `blocked_by_viewer`; capture both variants (`components/profile/ProfileScreenBody.tsx`).

### Table with pending Supper

1. **Fixture/test only:** seed a private Table using `table_members.member_id`, a filling `suppers` row, and `supper_members` rows where at least one member has no `entry_id` (`supabase/functions/entry/index.ts`).
2. Open `/tables`; capture the Supper card with a pending/ghost seat (`components/suppers/SupperCard.tsx`).
3. Open `/supper/[id]`; capture the roster and the viewer-empty-seat/add-take gate. Mock a rejected detail request separately for “not available”.

### Import in progress

1. **Fixture/test only:** inject an owner-bound local manifest in `reading` or `saving` phase through the same durable import-store helpers used by `hooks/wishlist/useProcessImportQueue.ts`.
2. Open `/import-progress`; capture active progress, then advance the mock manifest to review/digest/failed and capture each destination.
3. For a Maps list over 20, return the typed `large_list` envelope and exercise chunk progress in `lib/largeImportJob.ts`; mock background AppState to verify local completion notification without touching production.
4. For TikTok, test one high-confidence candidate for fast path and two candidates for escalation (`lib/importFastPath.ts`).

### Blocked-user views

1. **Fixture/test only:** insert `blocked_users(blocker_id=viewer, blocked_id=subject)` through the account test fixture.
2. Open `/u/[identifier]`; capture the blocked stub and unblock doorway.
3. Reverse blocker/blocked direction in a separate fixture and verify the server's visibility/error variant; do not infer one direction from the other (`supabase/functions/account/index.ts`, `supabase/functions/user-profile/index.ts`).

### Feed empty versus feed failure

1. Mock `feed-friends` with an empty successful `fn_friends_feed` page and open Following; capture the intended empty invitation.
2. Mock the same request failing before any cache exists; capture the retry state.
3. Seed cached cards, then fail the next page/refetch; confirm cards remain and only the incremental failure treatment changes (`components/feed/FollowingFeed.tsx`).
4. For For You, vary social/people/list queries independently to cover partial content, all-loading, any-error, and all-success-empty (`components/feed/ForYouFeed.tsx`).

### Places states and failure

1. Open `/places` with overlapping pinned/been fixtures. Capture the always-on segment header, default union with no chip lit, and bottom-left `list` control; narrow to pinned, release it to all, then switch to been. Move the camera, tap `list`, and confirm the map and sheet are absent while the paper ledger groups rows by city with counts, active locality first, count-desc remainder, and `ELSEWHERE` last. Load beyond 40 and verify only the final city count carries `+` until exhaustion. Tap the fixed `map` pill and confirm the prior camera returns. Separately reject each active source so intended-empty and cold broken-empty stay distinct; seed cached rows and confirm inline retry.
2. From browse peek, focus the field and capture the full paper takeover with RECENT / NEAR YOU / YOUR LISTS / PEOPLE YOU FOLLOW fixtures in that order. Confirm the map is unmounted, browse chips and import are absent, locality remains, RECENT is typographic, NEAR YOU uses only carried stored photos, lists use 44pt tinted plates, and followees use 56pt avatars plus first names. Repeat with zero follows and with the hidden flag. Repeat with every section empty and confirm the single muted search invitation. Type `parisik` and capture the result row; clear for sections, then use the back chevron to verify the union pins and saved camera return.
3. Enter a below-threshold query and confirm no restaurant request starts. With a valid query, capture spinner, zero results, cold error, and cached-row refresh error. Exercise a viewer rating, friends-tier rating, network-author rating, and unrated row; the first three use only Napkin data and the last leaves the cell empty. Confirm the selected-pin caption exposes no external rating numeral. Confirm browse peek leaves about 250pt visible including navigation.
4. Set locality to Paris while simulator coordinates remain in London; focus the empty field and confirm NEAR YOU is absent. Search `parisik` and confirm the result row has no distance token, no geocode request is issued, and the map remains unmounted until the back chevron restores the saved browse camera.
5. In browse, enter Lists at PEEK and capture Your lists → `new list` → Saved lists while the map and detent stay fixed. Drive loading, empty, my-lists cold failure, saved-lists partial failure, and populated states. Seed a saved query, then open `/(tabs)/places?mode=lists` without `q`; confirm the focused field is cleared and the same shelf appears. Enter one character for the threshold line, then two for public results. Scroll the shelf at FULL, return to offset zero, and verify a downward drag returns the sheet.
6. With People visible, enter it from peek and half: it must take over the screen, clear a stale query on a no-`q` mode arrival, unmount the map and sheet, issue no restaurant searches, and restore the prior detent and camera on exit. Scroll People away from zero and verify its native list. Repeat `mode=people` with the hidden flag in a test fixture for the two-segment Places fallback.
7. With fixture/local import state only, capture the clip doorway in four states without exercising a live write: resting pill + tray empty; `reading`/`saving` manifest with ring + clipping row; landed batch with two exhausted items showing count dot + matching amber row chip; and plain five-row history + `older ·`. Advance the local manifest into landed data while the tray remains open and confirm the row changes without reopening. Capture search mode with the pill absent and compare the search/chip y-offsets against all four browse frames. Open a landed row, then go back and confirm Places returns with the tray closed. Verify URL/video/screenshot write paths in isolated tests only.
8. Select friends with rated and unrated fixture network pins; capture the avatar face, rated row (`4.7 · clara` plus headcount metadata), unrated row (`clara` alone in the right column), selected caption, and restaurant route. Confirm there is no external rating numeral or provider suffix. Drive cold empty/error and cached refresh-error states, then release friends to confirm all restores without friend pins.
9. Load more than 40 pins and drive the FULL all/pinned ledger to exhaustion, confirming `40+` before the next page and an exact count after. Include one coordinate-less pin and open its repair sheet. Under pinned, narrow a facet and confirm the handoff still reports the full loaded pinned total.
10. Open the who picker and capture `you`, `friends`, and Table rows with only cached counts; select a Table and capture its name plus two-face chip, overlap pins, pinned ledger rows, and separate `been` gathered layer. From the Table tab, use the `on the map` row to push `/places-scope`, confirm the picker/import/bottom nav are absent, change layer/view/camera, and back to the same Table. Re-open the Places tab and confirm its prior who scope/view/camera survived. Open a notification destination in a test fixture and confirm it lands on the Places pinned list. Onboarding completion now opens the welcome before its Places destination.

### Gather expired/rescue state

1. **Fixture/test only:** return an expired, unrescued Gather hosted by the viewer from EF `gatherings?action=get`.
2. Open `/gathering/[id]?rescue=1`; the rescue Table sheet auto-opens once, backed by `useRescueGathering` (`app/gathering/[id].tsx`).
3. Mock a non-host and a `GONE_CODES` response separately to verify the rescue refusal and terminal-state treatment.

### Unmappable Map rows and denied location

1. Mock successful wishlist/list rows with null coordinates; open Map and the unmappable repair sheet (`components/wishlist/UnmappedSpotsSheet.tsx`).
2. Mock the same source request failing to distinguish broken-empty from unmappable data.
3. Set simulator location permission to denied and confirm the global map remains usable while nearby UI explains/requests permission (`hooks/useNearbyLocation.ts`).

### Reviews and visits verification — 5 September 2026

Native iPhone 16e / iOS 26.2 dev client evidence: live restaurant and All reviews read-only; isolated local query/mutation fixtures for bare/repeat visit, older-visit selection, combined rating/text/photos, optional calendar and cleared date, stable history, error/retry/undo, inline review photos and gallery overflow, more/less, empty/error/loading, own bare Feed among peers, and undated Diary. Screenshots: `.kanban/screens/reviews-visits/`. Fixture routes and adapters were removed after capture; production writes were never exercised. RPC writes and competing updates are covered by `scripts/test-visits-native.sh` and the visit SQL specs; real schema replay runs in migration CI.

### Onboarding verification — 13 September 2026

Native iPhone 17 Pro / iOS 26.2 dev client (`com.majilaii.dining-journal-app`), with isolated local auth, query and mutation fixtures: name entry; mandatory photo empty/approved/checking/rejected/service-unavailable; city suggestions with the software keyboard, selected city, completion pending and retry; optional follows populated/loading/completion error; all three welcome chapters, guide directory and chapter deep links, zero-Table introduction, contextual tip and dismissal. Standard and accessibility-medium text were captured; the photo control remains reachable by scrolling at the larger size. System dark appearance preserves the app's existing forced-light design. The terminal welcome action reached the isolated Places destination. Screenshots are in the primary checkout's `.kanban/screens/onboarding-overhaul-2026-09-13/`; fixture routes, provider adapters and Metro overrides were removed before final checks. No production writes were exercised. Automated tests cover server-confirmed completion, preview/deep-link enrollment gates, same-instance topic navigation, per-user persistence/races, ownership/scope placement, and the cold Table error/loading distinction.


### Onboarding follow-up — 2026-09-13

Native verification used the iPhone 17 Pro (402×874) and a disposable iPhone SE (375×667), iOS 26.2, with the installed development client and isolated local auth/query/mutation adapters. City search showed five long suggestions above the software keyboard without scrolling on both sizes; fifth-result selection, cancel/reopen, free text, no match, completion error/pending, and accessibility-medium scrolling were checked. Table creation covered the empty placeholder, typed name, mutual selection, pending/error, and keyboard footer; a page-sheet coordinate offset fixes the SE keyboard clipping the action. The iOS sharing chapter exposes its demo in the fixed footer, and both source choices, every handoff step, review selection/zero-selection refusal, result map, completion/close and replay were driven. Example controls never perform a real import or save. Screenshots live in the primary checkout’s `.kanban/screens/onboarding-followup-2026-09-13/`; only `*-fixed.png` is final evidence where a compact Table keyboard screenshot has an earlier counterpart. Temporary QA routes/provider adapters and Metro overrides are restored before final automated checks.


### 2026-09-13 — sharing emulator correction

Replaced the paper instruction-card demo with a full-screen TikTok/Instagram facsimile, source-specific sharing drawer, native-style iPhone share sheet and Apps list, followed by the Napkin extension, returned-source confirmation, clip tray, selectable review and illustrative map. Source artwork, target rings and Pressables share one layout; the rehearsal contains no copied private contacts or footage and performs no real import or save. The welcome artwork previews the actual new drawer. The sharing guide now distinguishes TikTok More from Instagram Share to. Existing iOS-only routing, intro completion, replay and Android exclusion remain intact.

Native evidence is in the primary checkout’s `.kanban/screens/share-emulator-2026-09-13/`: the 17 Pro covers both sources, every target, source return, clip tray/review/map and welcome replay; SE covers every TikTok handoff at default text size. Accessibility-medium SE checks use `se-large-*-fixed.png` where present: the extension keeps add for review outside the scrolling text, the tray prioritizes its review target, and the result map can scroll while completion remains visible. Earlier frames without `-fixed` are pre-fix evidence when a counterpart exists. Source/iOS artwork has fixed typography and hidden decorative accessibility nodes; action labels and step announcements remain accessible, while Napkin body/action text scales. The source scene is a still, not video playback. `instagram-preview.mp4` is a short recording of the native interaction; it is review evidence, not an app asset. Temporary native QA routes, adapters and Metro overrides are restored before final checks and shipping.

### Sign in with Apple — Guideline 4 fix, 2026-09-14

Native iPhone 16e dev client (`com.majilaii.dining-journal-app`), read-only against live data using the provisioned review identity; no writes were exercised and the account's entries, Top Four and settings are unchanged.

Captured: `/auth` signed out, showing Apple's own `AppleAuthenticationButton` — Apple artwork, mixed-case "Continue with Apple" in SF Pro, black style, pill corner radius, same 52pt height as and positioned above the Google button (the HIG prominence rule). The old custom Pressable rendered an Ionicons glyph and, through `Type.label`'s `textTransform: 'uppercase'`, the string "CONTINUE WITH APPLE"; the Google button directly beneath still shows that uppercase treatment, so the two together are the before/after.

Captured: the onboarding name step for an identity with no provider-supplied name — the standard `Make yourself at home / 1 of 3` header, Continue enabled against an empty field, `Maybe later` present — and `Maybe later` advancing to the photo step with the avatar falling back to `Your journal`. This is the path an App Review reviewer hits, because Apple withholds the name on every authorization after the first. The complementary skip path (a provider name present, so the step never mounts) is asserted in `hooks/onboarding/__tests__/useProvidedDisplayName.test.ts` rather than driven live, since seeding a provider name on a live account would be a write.

Screenshots: `.kanban/screens/siwa-guideline4-2026-09-14/`. The onboarding step was reached through the existing Settings → `show onboarding on launch` device preference, which routes an already-onboarded user through the stack while `useFinishOnboarding` short-circuits to `/welcome?preview=1` without a server write; the preference was switched back off afterwards.

Re-driven after adversarial review. The first capture of the name step wore the header `People
you know / Optional`: it had been passing SetupFrame's `optional` prop, which is
OnboardingProgress's follows-step copy switch rather than a general flag. The prop was removed and
the screen re-shot; `02-onboarding-name-optional.png` is the corrected frame. Four further
defects the review confirmed were fixed in the same pass: `Maybe later` shared `Continue`'s
handler and so saved a half-typed name instead of discarding it; an unbounded `updateUser` was
awaited between sign-in and `resumeAfterAuth`, racing RootLayoutNav's redirect; the auto-skip's
`router.replace` left the photo step with a back control pointing at nothing; and a skipped name
persisted the `'New User'` placeholder as the account's display name (first patched by deriving a name from
the login email, then reverted in review round 2 — completion stamps `account_privacy = 'public'`, so
that would have published an identity the user had just declined to give).

Review round 2 (automated PR review) found three more, all fixed: the resolved-name cache in
`useProvidedDisplayName` was not keyed by user id, so an auth-identity change could hand the
incoming account the previous one's name for a render or two — and the name step patches whatever
it is given straight into the draft; the local identity stash was still being awaited before
`resumeAfterAuth` despite `stash()` setting its memo synchronously; and the email-derived name
described above was reverted outright. The cross-account regression test was checked against the
unfixed code first — its initial version passed either way, because a name in `user_metadata`
resolves through the synchronous path and never populates the cache the guard protects.
