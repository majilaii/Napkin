/**
 * Placeholder screen for the Log tab.
 * The tab press is intercepted in _layout.tsx to open the create-entry modal instead.
 * This file exists only because expo-router requires a file for each tab.
 */
import { View } from 'react-native';

export default function LogPlaceholder() {
    return <View />;
}
