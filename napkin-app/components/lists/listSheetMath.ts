// Compatibility seam for older imports. The implementation now lives in the
// neutral sheets module and stays byte-for-byte equivalent to TICKET-186.
export * from '@/components/sheets/snapSheetMath';
